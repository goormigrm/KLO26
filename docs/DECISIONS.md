# 결정 기록 — KLO26

> 설계 단계에서 무엇을 왜 정했는지. 설계 자체는 [DESIGN.md](DESIGN.md) 가 정본이고, 여기는 **갈림길과 근거**만 남긴다.
> 사용자가 답한 것은 "결정", Claude 가 기본안으로 둔 것은 "기본안" 이다. 기본안은 답을 주면 바뀐다.

---

## 0. 전제 — 사용자가 못박은 것 (2026-09-08)

| # | 전제 | 설계에 미친 영향 |
|---|---|---|
| P-1 | **서버 없음 · DB 없음.** 철FPS(bedorage-duck)처럼 방을 만들고 P2P 로 붙는다 | 카드 획득·이적시장·랭크가 전부 빠졌다. 검증은 "받는 쪽이 규칙을 다시 센다"로만 한다 |
| P-2 | **팬 게임.** 문제 소지 없이, 광고·유료 결제 없이 | 엠블럼·사진·유니폼·타 게임 명칭 금지. 돈이 오가는 시스템 전부 제거. 비용 원칙(카드 등록 서비스 배제) |
| P-3 | GitHub 에 올려 공개 개발 | Vite + Actions + Pages. 저장소는 공개여야 Pages 가 무료다 |
| P-4 | `km`(KM26)·`KMD26` 을 참고 | 선수 데이터·전술 어휘·코드 규격·문서 체계를 이어받는다 |
| P-5 | 카드형 온라인 축구 게임 형식을 기반으로, **2026 K리그 구단 선수만** | 단일 클래스(2026-09-09 기준 1,056명). 시즌 카드·ICON 없음 |

---

## 1. 결정 (사용자 답변 · 2026-09-08)

### Q1. 경기를 어떻게 치르나 → **직접 조작 (실시간 액션)**

| 선택지 | 무엇 | 비용 | 판정 |
|---|---|---|---|
| 감독 모드 + 실시간 개입 | KM26 엔진(결정론)을 양쪽에서 돌리고 전술 변경·교체만 락스텝으로 | 수 주 (엔진·넷코드 재사용) | 권장했으나 **미채택** |
| **직접 조작** | 공을 가진 선수를 실시간 조작하는 11v11 액션 | 수 개월 (물리·AI·렌더 신규) | **채택** |
| 감독 먼저 → 조작 2단계 | 둘 다 | 둘의 합 | 미채택 |

**영향**: KM26 `MatchSim` 을 쓰지 않는다. 시뮬 코어(`src/core`)를 새로 만들되 결정론 규칙·넷코드는 bedorage-duck 에서, 선수 데이터·포메이션 좌표·능숙도 페널티 계수는 KM26 에서 가져온다. 감독 모드는 백로그(DESIGN 13장).

### Q2. 선수 카드를 어떻게 갖게 되나 → **전원 개방 + 급여 상한**

| 선택지 | 무엇 | 판정 |
|---|---|---|
| **전원 개방 + 급여 상한** | 카드 전부 사용. 급여 상한·팀컬러·강화 예산이 제약 | **채택** — 상대가 규칙 준수를 그 자리에서 검증할 수 있다 |
| 로컬 성장 (BP·팩·강화) | 브라우저 저장으로 수집·강화. 검증 불가 | 미채택 |
| 둘 다 | 공식전은 개방, 친선전은 성장 | 미채택 (범위) |

**영향**: DESIGN 5장 전체. "강화"는 확률이 아니라 **예산 배분**이 됐다. 카드 게임의 재미는 "무엇을 포기하고 누구를 쓰나"에서 나온다.

### Q3. 저장소·디렉터리 이름 → **KLO26**

KM26 → KMD26v1.0 → KLD26 의 결. 배포 주소 `https://goormigrm.github.io/KLO26/`. 게임 제목은 **개리그 온라인 2026**.

---

## 2. 기본안 — Claude 가 정한 것 (답을 주면 바뀐다)

| # | 항목 | 기본안 | 근거 | 대안 |
|---|---|---|---|---|
| D-1 | 시점·렌더 | ~~2D 탑다운 캔버스~~ → **Three.js 방송 카메라** (사용자 결정, 3장 6) | 방송 시점. bedorage-duck 의 3D 전환 방식(프리미티브 조립·툰 셰이딩·시뮬 불변)을 그대로 | 2D 탑다운 — 가벼운 모드로 백로그 |
| D-2 | 틱 | 60Hz 락스텝 | bedorage-duck 검증된 값. 22명 물리는 틱당 0.5 ms 안 | 30Hz (지터에 강하지만 조작감 하락) |
| D-3 | 경기 시간 | 전후반 **실시간 3분** (시계 ×15) | 6분 경기 체감 | 방 옵션 2·4분 |
| D-4 | 무승부·연장 | 무승부 허용, 연장·승부차기 없음 | 전적을 저장하지 않으니 승부를 억지로 낼 이유가 없다 | 방 옵션으로 승부차기 (백로그) |
| D-5 | 오프사이드 | 기본 켬, 방 옵션으로 끔 | 축구다 | — |
| D-6 | 교체 | 명령은 아무 때나, **다음 데드볼에 적용** | 경기를 멈추지 않아 락스텝이 단순 | 일시정지 교체 (양쪽 합의 필요 → 복잡) |
| D-7 | 강화 | 예산 24 · 카드당 +5 · +1 = 능력치 40종 +1 | 검증 가능. 효과 측정은 단계 7 | 수치는 12장 기준으로 조정 |
| D-8 | 팀컬러 | 5·7·9·11명 → +1·2·3·4 | 구단 팀컬러 계단 | 국가·리그 컬러 없음 |
| D-9 | 급여 상한 | **도구가 데이터로 정한다** — 올스타 18 은 밖, **29개 구단(K리그2 포함)** 순수 팀은 안 | "손으로 정한 숫자"를 믿지 않는다. K리그2 포함은 사용자 결정(3장 4) | — |
| D-10 | 선수 이름 | ~~실명~~ → **보호명** (사용자 결정, 3장 1). KM26 `fictName` 규칙을 빌드 단계에서 적용 | 저장소·화면에 실명이 남지 않는다 | 실명 — 원작자 방침과 같지만 채택하지 않았다 |
| D-11 | 외국인 제한 | 없음 | 카드 게임이지 시즌 규정이 아니다 | — |
| D-12 | GK 조작 | 없음 (자동) | 조작 복잡도. v1 범위 | PK·1대1 전진 키 (예약해 둠) |
| D-13 | 방 입장 | 방 목록만, 초대 링크·코드 입력 없음 | bedorage-duck 2026-09-05 결정 그대로 | — |
| D-14 | 전적 | 저장 안 함 (브라우저에도) | bedorage-duck 8.4 와 같은 이유 | 로컬 전적 (백로그) |
| D-15 | 키 배치 | ~~WASD + J K L I~~ → **축구 게임 표준 키 그대로** (사용자 결정, 3장 8) — 방향키 이동 · S 패스/선수 변경 · W 스루 · A 로빙/슬라이딩 · D 슛/압박 · E 대시 · Space 태클 · C 견제 · Q 팀 압박·조합 · Ctrl 페이스 컨트롤 · `[ ]` 프리셋 | 축구 게임 하던 사람이 그대로 한다 | 설정에서 바꾼다 |
| D-16 | 인원 | 1v1 만 (정원 2) | 2v2 는 조작 분배 설계가 따로 필요 | 백로그 |
| D-17 | 특성 54종 | v1 은 표시만 | 시뮬 반영은 계측이 먼저 | 단계 7 에서 일부 |

---

## 3. 추가 결정 (사용자 답변 · 2026-09-08)

| # | 질문 | **결정** | 영향 |
|---|---|---|---|
| 1 | 선수 이름 — 실명 / KM26 보호명 | **보호명** | D-10 뒤집힘. `build_cards.py` 가 `fictName` 규칙(성씨 치환·외국인 초성 시프트·충돌 회피)을 빌드 단계에서 적용. 저장소에 실명 0건이 단계 1 검토 기준 (DESIGN 5.1 · 10.2 · 11장) |
| 2 | 경기 시간 기본값 | **3분** | D-3 확정. 방 옵션 2·4분 |
| 3 | 게임패드 | **키보드로만** → 이어서 **"게임패드는 개발하지 않는다 — 계획에서 제외"** (같은 날 재확인) | 백로그에서도 뺐다 (DESIGN 1.3 · 3.2 · 13장). 첫 화면·README 에 "PC 키보드 전용 · 게임패드 없음" 명시 |
| 4 | 급여 상한 조건 2 를 K리그2 까지 넓힐지 | **K리그2 까지** | 29개 구단 전부 자기 선수 18명으로 상한 안 (DESIGN 5.4). 도구가 29개 전부 센다 |
| 5 | 공개 채널 | **공개 시점에 정한다** | PREP B-3 그대로 열어 둔다 |
| 6 | 시점·렌더 (사용자가 먼저 꺼냄) | **Three.js 방송 카메라.** 선수 **키 반영**. 피부색은 가능하면(필수 아님). 생김새는 **귀여운 찰흙** | D-1 뒤집힘. DESIGN 7.1 다시 씀 — 사이드라인 상공 카메라 · 프리미티브 조립 찰흙 선수 · `h/180` 키 배율 · `sqrt(w/75)` 굵기. **피부색은 데이터가 없어 안 한다**(억측 금지, 백로그). 생김새는 닮게 그리지 않고 머리 모양만 id 해시로. `package.json` 에 `three` 추가. 단계 3 비용이 커졌다(bedorage-duck 은 3D 전환에 2~3주) |

| 7 | 폰 (사용자가 먼저 꺼냄) | **PC 전용. 폰은 막는다** | 터치 조작을 만들지 않는다. 폰·태블릿은 안내 화면만 (DESIGN 3.4 · 7.2). 단계 3 범위가 줄었다. PREP B-2(실제 폰 확인) 삭제 |
| 8 | 키 배치 (사용자가 먼저 꺼냄) — "지금 키는 축구 게임에 맞지 않는다" | **축구 게임 표준 조작법 그대로** | D-15 뒤집힘. DESIGN 3.1 표 다시 씀 — 구현 가능한 것(방향키·S/W/A/D/E·Space·C·Q·Ctrl·조합 Q+D/Q+A/A+A·`[ ]`)은 같은 키, 못 하는 것(감아차기·개인기·F 강슛)은 표에 "v1 없음"으로. `src/core/input.ts` 비트도 키 이름 그대로 |
| 9 | 고지 문구 (사용자가 먼저 꺼냄) — "이런 문구 자체가 문제가 된다" | **제3자를 거명하는 고지 문구 전부 삭제.** 소스·문서에 꼭 필요한 정보만 | README 고지 절 삭제 · NOTICE.md 삭제 · 첫 화면 문구 축소 · 타 게임·회사 이름을 문서 전체에서 걷어냄(형식·키 배치는 "카드형 축구 게임"·"표준 키 배치"로). DESIGN 10.2 는 "고지 대신 안 쓰는 것으로 답한다"로 다시 씀 |

## 4. 확인 필요 (단계 3 에서 생긴 것 · 2026-09-09)

| # | 질문 | 지금 상태 | 사용자가 정할 것 |
|---|---|---|---|
| C-1 | Ctrl+W 가 크롬 탭을 닫는다 | **해결됨 (사용자 결정 2026-09-09)** — 페이스 컨트롤을 **Shift** 로 옮겼다. 설계 3.1 표와 다른 유일한 키다 | — |
| C-2 | 리스타트 킥 조작 | **구현됨** — 방향키 + S/W/A/D. D 는 홀드해서 놓는다(파워). 스로인은 손, 골킥의 D 는 길게 차기. 방향키가 없으면 AI 가 고른다 | 해 보고 |

---

## 5. 축구 규칙 · 밸런스 (사용자 지적 2026-09-09 → 단계 4)

| # | 사용자가 지적한 것 | 어떻게 했나 |
|---|---|---|
| R-1 | 킥오프 전에 상대 진영에 있으면 안 된다 | 전원 자기 진영 · 차지 않는 팀은 센터 서클 밖. **매 틱 강제**해서 사람이 조작해도 못 어긴다 (`enforceRestartPositions`) |
| R-2 | 골키퍼가 손에 든 공을 뺏을 수 있었다 | 슬라이딩·압박 도전 자체가 **파울**(골키퍼 차징). 상대 프리킥 + 경고 확률 |
| R-3 | 골키퍼가 공을 멀리 못 찼다 | `gkPunt` 추가 (55~72 m, `kic` 반영). 골킥의 D 는 슛이 아니라 길게 차기 |
| R-4 | 스로인을 발로 찼다 | `doThrow` — 손으로. 최대 14 m/s · 반드시 뜬다 · **직접 골 무효** · 던진 사람 두 번 터치 반칙 |
| R-5 | 심판이 없다 | 주심 1 + 부심 2 (`src/render3d/referee3d.ts`). **렌더 전용**이라 결정론에 끼지 않는다. 카드·오프사이드 깃발 |
| R-6 | 1대1 인데도 골이 안 들어간다 | 원인 둘. ① 사람 슛 자동 보조가 **정중앙(=골키퍼 자리)**을 겨눴다 → 골키퍼가 비운 코너를 기본값으로. ② 골키퍼가 반경 2.2 m 안이면 무조건 시도했다 → **반응 시간 + 몸 날릴 시간**으로 모델링. 계측: 1대1 14 m 전환율 **39%**, 8 m **58%** |

### 단계 7 계측으로 잡은 것 (2026-09-09)

| 무엇 | 어떻게 드러났나 | 고친 것 |
|---|---|---|
| **압박 슬라이더가 연결돼 있지 않았다** | 지문 검사에서 5시드 중 1시드만 달라졌다 (`d < dPress + 10` 이라 0 이든 4 든 늘 참) | 붙는 거리 · 압박 인원(1~3명) · 태클 시도 여부를 함께 움직인다 → 5/5 |
| **멘탈리티가 지표를 안 움직였다** | 짝 비교 38% | 앵커 이동 ±4 → ±6 m, 전진 패스 가중 ×0.6~1.4 |
| 오프사이드가 판당 11.5회 | 계측 (실제 축구는 2~4회) | AI 가 라인을 1.5 m 여유로 지키고, 오프사이드 위치 동료에게는 패스하지 않는다 → 1.0회 |

---

## 6. 실제 데이터 (2026-09-09 · 원작자 허락 받음)

| # | 갈림길 | **결정** | 근거 |
|---|---|---|---|
| N-1 | 선수 이름 | **보호명** — KM26 `fictName` 그대로 (성씨 치환 · 외국인 초성 시프트) | D-10·3장 1 의 결정을 실제 데이터에 적용. 부르는 느낌은 살고 실존 인물과는 겹치지 않는다 |
| N-2 | 보호명이 **다른 실존 선수**의 이름이 되면 | 마지막 글자 초성을 밀어 피한다 (3건 발생) | 정호진 → 진호진 인데 진호진이 실제로 명단에 있으면 보호가 아니다 |
| N-3 | 구단 이름 | **지역명만 남기고 기업명·구단 고유 별칭을 뺀다** (FC 안양 · 울산 FC · 전북 FC …). `build_cards.py` 의 `CLUB_NAME` 표 한 곳 | 사용자 요청("일부 변형된 형태") + DESIGN 10.2 "고지 대신 안 쓰는 것으로 답한다". 지역명은 공공 지명이라 그대로 둔다 |
| N-4 | 구단 색 | **그대로 쓴다** — ~~유니폼 상의 `col` · 소매/바지 `col2`~~ → **2026-09-10 부터 `col` 단색**(G-6). `col2` 는 데이터에만 남고 화면·유니폼에서 안 쓴다 | 색은 상표가 아니고, 22명을 구별하려면 필요하다. 실제 유니폼 **디자인**은 안 쓴다 |
| N-5 | 급여 상한 조건 2 의 계산 | **포지션을 가리지 않은 최고 18명** | 포지션 몫으로 재면 포메이션에 따라 상한을 넘는 스쿼드가 생긴다 (강원 4-3-3 이 231 > 230). 상한 230 → **238** |

---

## 7. 소리 · 테스트 모드 (2026-09-09)

| # | 갈림길 | **결정** | 근거 |
|---|---|---|---|
| S-1 | 배경음·효과음을 어디서 구하나 | **전부 코드로 만든다** (Web Audio 오실레이터·노이즈·필터). 음원 파일 0개 | 사용자 요청("저작권 없는 것"). 무료 음원도 출처·라이선스를 따라다녀야 하는데, 만들어 쓰면 그 문제가 아예 없다. DESIGN 7.3 과도 같다 |
| S-2 | 소리 크기 | **파형 RMS 로 재서 맞춘다** | 귀로 맞추면 사람마다 다르다. 골대가 다른 소리의 3~6배로 튀고 스로인이 관중석에 묻히던 것을 계측이 잡았다 |
| S-3 | 배경음의 성격 | 로비는 **화음 패드 + 아르페지오**, 경기는 **관중석 웅성거림**(공이 골문에 가까울수록 두꺼워진다) | 경기 중 음악이 깔리면 축구가 아니다. 대신 관중이 긴장을 알려 준다 |
| T-1 | 테스트 모드를 어떻게 가리나 | **`?test=1` 주소 인자** | 사용자 요청("배포 때는 가릴 것"). 설정 플래그보다 확실하다 — 링크를 안 걸면 끝이고, 코드를 지울 필요도 없다 |
| T-2 | 테스트 모드에 무엇을 넣나 | **AI 대 AI 관전** + **키 입력 표시** | 관전은 AI 를 오래 지켜봐야 보이는 것(라인·압박·세트피스)을 보려고, 키 표시는 입력이 실제로 sim 에 닿는지 보려고 |
| U-1 | 로비 구조 | 큰 버튼 셋 → **팝업** (bedorage-duck 방식) | 사용자 요청. 옵션이 늘어나 한 화면에 다 펴 놓으니 첫 화면이 무거웠다 |

---

## 8. 스쿼드 화면 (2026-09-09)

| # | 갈림길 | **결정** | 근거 |
|---|---|---|---|
| Q-1 | 저장된 스쿼드가 데이터 변경을 견디게 하려면 | **데이터 지문을 같이 저장하고 다르면 버린다** | id 만 저장하면 카드 데이터가 바뀔 때 딴 선수를 가리킨다 (골키퍼 자리에 공격수). 스쿼드 코드는 이미 그렇게 하고 있었다 |
| Q-2 | 골키퍼 자리를 어떻게 지키나 | **화면에서 아예 못 놓게 막는다** (규칙 검사에만 맡기지 않는다) | 검사만 하면 "규칙 위반" 딱지를 보고 스스로 고쳐야 한다. 애초에 못 놓게 하는 편이 낫다 |
| Q-3 | 스쿼드를 어디서부터 짜나 | **구단을 먼저 고르고 자동으로 채운다** | 사용자 요청. 빈 판에서 18명을 고르는 것보다, 내 구단으로 시작해 몇 명만 바꾸는 쪽이 훨씬 쉽다 |
| Q-4 | 선수 목록의 기본 범위 | **내 구단만.** "전 구단"은 버튼 하나로 | 사용자 요청("구단 자산 내에서"). 전원 개방(Q2)은 그대로 살아 있고, 기본값만 구단으로 좁힌 것이다 |
| Q-5 | 자리 옮기기 방식 | **눌러서 맞바꾸기 + 끌어다 놓기** 둘 다 | 클릭은 어디서나 되고, 드래그는 익숙하다 |

## 9. 사용자 제보 11건 (2026-09-10 — 직접 해 보고 낸 것)

| # | 제보 | **어떻게 했나** | 어디 |
|---|---|---|---|
| F-1 | 골키퍼가 가운데 서 있는데 옆으로 찬 공이 손으로 빨려 들어간다 | 잡기를 **실제 손 거리(서서 0.8 · 날면서 1.0 m)** 로 제한. 경로가 손 밖이면 반응 뒤 그 점으로 **몸을 날리고**(속도 있는 다이브), 못 닿으면 들어간다. 공 순간이동 없음 — 테스트가 한 틱 0.6 m 를 본다 | `ball.ts gkCatch` · `sim.ts`(다이브 속도 유지) · DESIGN 4.7 |
| F-2 | 방향키에 따라 패스·슛 방향을 `>` 로 표시 | 조작 선수 앞 1.3 m 에 `›` (렌더 전용). 공격 노랑 · 슛 모으면 붉고 길어짐 · 수비 흰색 | `renderer3d.ts` · DESIGN 3.3 |
| F-3 | 킥오프 때 상대 진영으로 차는 등 규칙 위반 | **킥오프는 아군에게 짧은 땅볼 패스로만**(D 를 눌러도). 차는 순간 휘슬+자막. **백패스 룰**(아군이 발로 준 공은 GK 가 손으로 못 잡음) 추가. 현대 규칙은 전방 킥오프를 허용하지만 사용자 결정 | `rules.ts kickoffTarget` · `ball.ts gkCatch` · DESIGN 2장 |
| F-4 | 태클·압박이 공 잡은 선수를 향하지 않는다 | **D 홀드 = 공을 향해 스스로 달린다**(방향키는 45% 옆으로), **Space = 공 쪽 돌진 + 12틱 ×3**, **A = 방향키 없으면 공 쪽으로 슬라이딩**, **C = 마주 보기 + 들이받는 드리블 ×1.5** | `sim.ts` · `ball.ts contestBall` |
| F-5 | 아래 키 + 슛이 골문을 통째로 벗어난다 | **슛은 언제나 골문을 겨눈다.** 방향키 위/아래가 코너, 안 누르면 GK 가 비운 코너. 뒤로 밀고 차면 오차 ×1.6 만 | `ball.ts doShoot` · DESIGN 3.3 |
| F-6 | 추가시간 없이 뜬금없이 후반으로 넘어간다 · 킥오프 휘슬/자막 | 죽어 있던 시간을 모아 **추가시간 1~5분 발표**(`45'+2`). 추가시간 뒤 **공이 죽었을 때** 끝내고, 공격 중이면 최대 20초 기다림. `whistle` 사건은 실제로 차는 순간에만. 자막 "전반/후반 킥오프 · 전반 종료 · 추가시간 +N분" | `rules.ts advanceClock` · `hud.ts` · `sfx.ts` |
| F-7 | 수비 강화 · 수비 키 세분화 | 수비 능력치가 실점을 줄이는 길 여섯(DESIGN 4.8 표: 가로채기 반경/성공·블록·마크 거리·압박 예측·견제). 수비 키에 **W = 골키퍼 돌진** 추가 | `ball.ts` · `ai.ts` · DESIGN 3.1 |
| F-8 | 선수를 조금 더 마르게 | 몸통·팔다리 굵기 ×0.8 (`SLIM`), 몸통 살짝 길게. 머리는 그대로 | `player3d.ts` |
| F-9 | 공지글·플레이 가이드 · 커밋 전 문서 갱신 | `docs/플레이-가이드.md` 신설 · `docs/공지글-모음.md` 1차 문안 · 이 문서·DESIGN·CHANGELOG·HANDOVER·README 를 같은 커밋에 | docs |
| F-10 | 대기실·로비 버튼 보기 좋게 · 저장 슬롯 위로 5개 · **급여 강조** | 로비 스쿼드 띠에 구단 색·급여 게이지·큰 버튼. 스쿼드 화면 맨 위에 **저장 슬롯 5칸 띠**(불러오기/덮어쓰기/지우기). 급여를 큰 숫자로, **남은 급여**를 적고, 선수 목록마다 **바꾸면 ±얼마**와 **상한 초과**를 표시(초과는 못 고른다). 대기실은 양쪽 구단 색·선발 OVR·급여·팀컬러·강화 | `squad.ts` · `lobby.ts` · `waitroom.ts` · `style.css` |
| F-11 | 강화 시스템이 뭔지 모르겠다 | 강화 상자에 설명("+1 = 능력치 전부 +1, OVR 63 → 지금 65 → 다음 66, 급여 안 오름"), 남은 예산, **자동 배분 셋**(선발 고르게 · 공격에 몰기 · 전부 0) | `squad.ts` |

## 10. 스쿼드 화면 · 유니폼 (사용자 요청 2026-09-10 두 번째)

| # | 요청 | **어떻게 했나** | 어디 |
|---|---|---|---|
| G-1 | 자동 채우기를 선발/후보로 나누고, 기준을 능숙도 우선 / 능력치 우선으로 고르게 | 띠 하나 — **선발 11명 · 후보 7명** 버튼 + 기준 세그먼트(브라우저에 기억). 능숙도 우선 = `fam×2 + ovr`, 능력치 우선 = `ovr×2 + fam×0.3`. 선발을 채워 후보가 비면 모자란 포지션(GK1·DF2·MF2·FW2)부터 메운다. 내 구단 선수만 | `squad.ts autoFill` |
| G-2 | 정확한 OVR·세부 능력치를 숫자로 보이지 않게 — 별 5개 중 3.5개 식으로 | `ovrStars`(40→½ … 90→5) · `statStars`(20점에 별 1). 전술판 칩·후보·목록·상세·구단 카드·대기실·강화 상자 전부 별(반 개 단위). 급여·능숙도는 숫자 그대로(규칙이라 알아야 한다) | `cards.ts` · `ui/stars.ts` |
| G-3 | 목록에 능력치 숫자만 있고 항목명이 없다 | 목록 머리줄에 **PAC SHO PAS DRI DEF PHY**(GK 필터면 DIV HAN KIC REF POS SPD, 마우스 올리면 우리말) + 행마다 **미니 막대 6개**(숫자 없음). 상세는 항목명 + 우리말 + 별 | `squad.ts cardRow` |
| G-4 | 코드 복사/붙여넣기 대신 JSON 저장·불러오기 | **JSON 저장**(파일 내려받기 `klo26-구단-포메이션.json`, 데이터 지문 포함) · **JSON 불러오기**(파일 선택 → 지문·규칙 검사). 코드는 화면에서 뺐고 P2P 대기실에서만 쓴다 | `squad.ts` |
| G-5 | 저장하지 않고 뒤로 가는 버튼 | **저장 안 하고 돌아가기** — 화면을 열 때의 스쿼드로 되돌린다(없었으면 저장을 비운다). "이 스쿼드로 (저장)"과 짝. 구단 고르기의 "로비로"도 같은 동작 | `squad.ts cancel` |
| G-6 | 두 색이 섞이지 않게 · 원정은 GK 빼고 상하의 흰색 | 홈 = `col` **단색**(상의·소매·바지·양말). 원정 = **전신 흰색**(GK 제외). 홈이 거의 흰색이면 그때만 원정이 자기 색. UI 의 구단 띠·대기실·로비도 단색 | `render3d/kits.ts` · `style.css` |
| G-7 (9/11) | 전술판에 이름이 안 보인다 · 세로로 돌려 달라 | **세로 전술판**(자기 골문 아래, 공격 위). 칩은 이름·별·자리 세 줄(86px), 가운데 자리는 4% 아래로 어긋남 | `squad.ts chip` · `style.css .pitch.vertical` |
| G-8 (9/11) | 능력치 막대를 우리말로, 읽기 쉬운 형식으로 | 머리줄 우리말 + **눈금 다섯 칸**(별과 같은 반 칸 단위). 올리면 "속력 3.5/5" | `stars.ts pipsHtml` |
| G-9 (9/11) | 자동 채우기를 전술판 바로 위로 | `.sq-board` 맨 위, 금색 테두리 띠 | `squad.ts` |
| G-10 (9/11) | 선수 클릭 시 별이 전부 만점으로 보인다 | 버그 — 별 span 이 그리드 칸에서 늘어나 덮개(폭 %)가 다 덮었다. `.stars { width: max-content }` | `style.css` |
| G-13 (9/11) | 홈이 방장 고정이면 안 된다 · 동전 던지기 연출 · 경기장도 홈 팀 컬러로 | **홈/원정을 경기 시드에서 유도**(`game/toss.ts tossHostHome`) — 그 자리에서 난수를 뽑으면 두 브라우저가 다른 홈을 본다. `waitroom.begin` 이 `(role==='host')===hostHome ? 0 : 1` 로 `me` 와 스쿼드 순서를 함께 정해 저절로 일치. 혼자 하기도 사람이 원정일 수 있다(`human`·`bots` 배열이 따라 뒤집힌다). 연출은 **렌더 전용** 3초 오버레이(온라인은 멈추지 않는다). 관중석·스탠드는 `pitch.setHomeColor()` 로 홈 색 | `game/toss.ts` · `session.ts` · `waitroom.ts` · `pitch3d.ts` · `tests/toss.test.ts` |
| G-12 (9/11) | 전 구단 선수 검색에 구단 선택 | "전 구단" 범위일 때만 **구단 셀렉트**(29구단 + 전 구단). 고르면 그 구단만 보이고 구분선에도 구단명. "내 구단" 일 때는 숨긴다 — 세그먼트와 겹치지 않게 | `squad.ts filterClub` |
| G-11 (9/11) | 선발·후보·그 외 명단을 개리그 매니저처럼 배치 · 전술판 아래 후보는 안 보이니 없애기 | KM26 `fmSquadTable` 을 따라 **한 통에 세 구역**(선발 11 자리순 → 후보 7 → 그 외 명단 스크롤), 같은 열·같은 행 모양. 전술판 아래 벤치 칩 제거. 줄 클릭 = 전술판과 같은 자리 선택, 버튼으로 `⬇ 후보로`·`⬆ 선발로`·`⬆ 후보로`. **그 외 명단은 스쿼드 18명을 뺀 나머지**(예전엔 흐리게 섞여 있었다). 예비 GK 경고도 KM26 대로 | `squad.ts rosterHead/slotRow/pinToBench` · `style.css .rrow` |

앞으로 생기는 갈림길은 이 문서에 번호를 이어 적는다.
