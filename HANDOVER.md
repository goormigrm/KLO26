# HANDOVER — 2026-09-09 (4차)

> **이 문서가 최신입니다.** 단계 1~7 이 전부 들어갔습니다 — K리그 2026 29개 구단 1,024명(보호명)으로
> 스쿼드를 짜서 **봇과도, 다른 사람과도** 한 판 할 수 있고 오프사이드·파울·카드·PK·교체까지 돕니다. 심판 3명이 뜁니다.
> **소리도 들어갔습니다** (전부 코드 생성 — 음원 파일 없음). 남은 것은 다른 회선 두 대 실제 대전(PREP B-1)과
> 사람 손으로만 알 수 있는 조작감입니다.

## 세션 메타데이터

| 항목 | 값 |
|---|---|
| 날짜 | 2026-09-09 |
| 작업 디렉터리 | 이 PC: `C:\Users\tkdrm\OneDrive\Desktop\klo26` · 다른 PC: `C:\Users\tkdrm\Workspace\personal\KLO26` |
| 저장소 | `https://github.com/goormigrm/KLO26` · `main` |
| 배포 | <https://goormigrm.github.io/KLO26/> — push 하면 Actions 가 테스트·빌드·배포 |
| 참고한 곳 | 배도라지덕(bedorage-duck) — 이 PC 에서는 `C:\Users\tkdrm\OneDrive\Desktop\철FPS`. KM26·KMD26 은 `Desktop/km26` · `Desktop/KMD26v1.0` |

두 PC 를 오가므로 다음 세션은 `git fetch origin && git status -sb` 부터.

---

## 지금 되는 것

| 단계 | 무엇 | 상태 |
|---|---|---|
| 2 | 결정론 시뮬 코어 (물리·AI·규칙) | ✅ |
| 3 | Three.js 방송 카메라 · 찰흙 선수 · 표준 키보드 · 폰 차단 · 혼자 하기 | ✅ |
| 4 | 오프사이드 · 파울/카드 · 프리킥/PK · 교체 · 세트피스 위치 강제 · **심판 3명** | ✅ |
| 5 | 카드 6스탯·OVR·급여 상한·팀컬러·강화 예산·스쿼드 코드·스쿼드 화면·저장 슬롯 | ✅ |
| 6 | Trystero 로비·방 · 2인 락스텝 · 해시/리싱크 · 대기실 | ✅ (로컬 두 탭 검증) |
| 7 | 밸런스 계측 (`npm run verify`) · 슬라이더 지문 · 짝 비교 · 효과 크기 | ✅ |
| **1** | 실제 K리그 2026 선수 데이터 (보호명) | ✅ **원작자 허락 받음 (2026-09-09)** |
| — | 소리 (Web Audio 절차 생성 — 음원 파일 없음) | ✅ |
| — | **테스트 모드** (`?test=1`) — AI 대 AI 관전 · 키 입력 표시 | ✅ |

**흐름**: 로비 → (스쿼드 수정 | 구단 바꾸기) → (혼자 하기 | 방 만들기/참가 → 대기실) → 경기 → 결과.

**스쿼드 화면 두 갈래** (`src/ui/squad.ts`, `mode: 'club' | 'board'`)
- `SquadScreen(host, onDone, 'edit')` — 저장된 스쿼드가 있으면 **전술판**부터. 로비 "스쿼드 수정".
- `SquadScreen(host, onDone, 'club')` — **구단 고르기**부터. 로비 "구단 바꾸기".
- 저장된 스쿼드가 없으면 `'edit'` 로 들어와도 구단 고르기로 떨어진다 (`loadSquad()` 가 `null`).
- 전술판에서 바꾸는 세 가지: 자리↔자리(두 번 클릭/드래그) · 선발↔벤치(`#quick-swap`) · 선수 교체(오른쪽 목록).
  `mark(i)` 가 `can`/`cant` 클래스를 붙여 **바꿀 수 있는 자리를 초록으로** 보여 준다.
  `bestPartner(i)` 가 선발↔벤치 짝을 자동으로 고른다. 두 곳 다 `canPlace()` 하나만 본다 —
  규칙(골키퍼 자리)을 고치려면 `canPlace()` 만 고치면 된다.

---

## 이번 세션에서 한 것

### 사용자가 지적한 축구 규칙 4건 + 심판

1. **킥오프 위치** — 전원 자기 진영, 차지 않는 팀은 센터 서클 밖. `enforceRestartPositions` 가 **매 틱 강제**하므로 조작으로도 못 어긴다.
2. **골키퍼 보호** — 손에 든 공은 슬라이딩·압박으로 못 뺏는다. 도전 자체가 파울(골키퍼 차징) + 경고 확률.
3. **골키퍼 롱킥** — `gkPunt` (55~72 m, `kic` 반영). 골킥에서 D 는 슛이 아니라 길게 차기.
4. **스로인은 손으로** — `doThrow`. 최대 14 m/s · 반드시 뜬다 · **직접 골 무효** · 던진 사람 두 번 터치 반칙.
5. **심판** — 주심 1(대각선 추적 · 카드) + 부심 2(오프사이드 라인 · 깃발). `src/render3d/referee3d.ts`, **렌더 전용**이라 결정론에 끼지 않는다.

### 밸런스 (사용자 제보 "1대1 인데도 골이 안 들어간다")

원인이 둘이었다.
- **사람 슛 자동 보조가 골문 정중앙을 겨눴다** — 방향키를 옆으로 안 밀면 골키퍼가 서 있는 자리로 갔다. 이제 **골키퍼가 비운 코너**를 기본값으로 잡고 스틱 옆 성분으로 옮긴다.
- **골키퍼가 반경 2.2 m 안이면 무조건 막았다** — 이제 **반응 시간 + 몸 날릴 시간**으로 닿는 거리를 정한다.

계측: 1대1 14 m 전환율 **39%**, 8 m **58%** (`npm run oneone`). 봇전 120판 평균 **2.72골** (목표 2.5~3.5).

### 단계 7 계측이 잡아낸 것 (이 방법론이 실제로 일했다)

| 무엇 | 어떻게 드러났나 | 고친 것 |
|---|---|---|
| **압박 슬라이더가 연결돼 있지 않았다** | 지문 검사 5시드 중 1시드만 달라짐 (`d < dPress + 10` 이라 늘 참) | 붙는 거리 · 압박 인원(1~3명) · 태클 시도를 함께 움직인다 → 5/5 |
| 멘탈리티가 지표를 안 움직였다 | 짝 비교 38% | 앵커 ±4 → ±6 m · 전진 패스 가중 → 슛 지표 64% |
| 오프사이드 판당 11.5회 (실제 2~4회) | 계측 | AI 가 라인을 1.5 m 여유로 지키고, 오프사이드 위치 동료에게는 패스하지 않는다 → 1.0회 |
| 패스 성공률 44% | 계측 | 받으라고 보낸 선수의 퍼스트 터치를 올렸다 (가로채기와 같은 확률이었다) → 51% |

---

## 검증 결과 (`npm run verify 200`)

| 항목 | 결과 | 기준 |
|---|---|---|
| 슬라이더 지문 (line·press·width·mentality) | **4/4 연결됨** | 전부 |
| 압박 ↑ → 상대 패스 성공률 ↓ | 81% ✅ | ≥62% |
| 압박 ↑ → 파울 ↑ | 69% ✅ | |
| 멘탈리티 ↑ → 우리 슛 ↑ | 64% ✅ | |
| 수비 라인 ↑ → 상대 오프사이드 ↑ | 62% △ | |
| 팀컬러 +4 | 승률 **+10.0%p** ✅ | 8~15%p |
| 강화 24 → **공격수** 5명 | +7.8%p △ | 8~15%p |
| 강화 24 → **수비수** 5명 | −0.3%p ❌ | — |
| 홈/원정 대칭 (같은 스쿼드) | 51.5% ✅ | 47~53% |
| 능숙도 초록 11 vs 빨강 11 | 82.8% ✅ | ≥70% |
| 같은 시드 100회 해시 | 100/100 ✅ | 100% |
| 봇전 120판 평균 골 | 2.72 ✅ | 2.5~3.5 |

**남은 숙제 하나**: 강화 예산을 **수비수에게 쓰면 승률이 안 오른다**(0%p). 공격수에게 쓰면 +7.8%p.
"어디에 쓰느냐가 갈린다"는 것 자체는 카드 게임답지만, 수비 강화가 **아무 값도 없는 것**은 수비 능력치가 경기에 덜 반영된다는 뜻이다.
단계 7 후속으로 볼 것 — 태클·마크·위치 선정이 실제로 실점을 줄이는지.

---

## 다음 단계

### 사용자가 먼저

1. **직접 한 판 해 보기** — 조작감·카메라·골 빈도·심판이 보이는지. 숫자로 못 재는 것들이다.
2. **구단명이 이대로 괜찮은지** — `tools/build_cards.py` 의 `CLUB_NAME` 표 한 곳이다. 지금은 지역명만 남기고 기업명을 뺐다.
3. **B-1 다른 회선 두 대로 대전** — 로컬 두 탭은 통과했다. NAT 는 같은 PC 로는 못 잡는다.

### 다음 세션

| 순서 | 무엇 | 끝났다의 기준 |
|---|---|---|
| 1 | 사용자 피드백 반영 (조작감·카메라·난이도·소리 크기) | 사용자가 "됐다" |
| 2 | 수비 강화가 값을 하게 (위 숙제) | `npm run verify` 에서 수비 강화 +4%p 이상 |
| 3 | 단계 8 공개 — 공지글 · 플레이 가이드 | `docs/공지글-모음.md` |

---

## 주의사항 & 교훈

앞 세션 것(1~21)은 그대로 유효하다. 이번에 더한 것:

22. **`src/core` 안에서 ball.ts 는 rules.ts 를 import 하지 않는다** (순환). 반칙은 `st.pending` 에 적어 두고 `sim.step` 이 끝에서 `resolvePending` 을 부른다.
23. **세트피스 위치는 매 틱 강제한다.** AI 한테만 맡기면 사람이 조작해서 규칙을 어긴다 (킥오프에 상대 진영 진입 등).
24. **오프사이드는 "표시 → 관여하면 판정"** 두 단계다. 패스가 나가는 순간 위치를 표시(`markOffside`)하고, 표시된 선수가 공을 먼저 만지면 그때 반칙이다. 스로인·골킥·코너는 `clearOffside` 로 지운다.
25. **교체는 선수 객체를 바꾸지 않고 `spec` 만 갈아 끼운다.** 22칸은 고정이고 "누가 그 등번호를 입고 뛰는가"만 바뀐다 — 인덱스가 안 깨져 락스텝·해시가 그대로다. 렌더는 `rebuildRig` 로 그 한 명만 다시 만든다.
26. **효과 크기는 반드시 짝 비교로.** 따로 돌리면 시드 잡음(±6%p)이 효과(+8~15%p)를 덮는다. 팀컬러가 처음엔 −4%p 로 나왔다가 짝 비교·200판에서 +10%p 로 드러났다.
27. **Trystero 의 `makeAction<T>` 는 `DataPayload` 제약이 있다.** 우리 메시지 타입은 그 제약을 못 맞추므로 `makeAction('ctl') as unknown as […]` 로 감싼다.
28. **Claude 브라우저 패널의 배경 탭은 창이 좁다** — 폰 차단(900px)에 걸린다. 두 탭 대전을 시험하려면 `resize_window` 로 넓혀야 한다.
29. 온라인 대전은 **일시정지가 없다.** 내가 멈추면 락스텝 때문에 상대도 멈춘다.
30. **`prompt()`·`navigator.clipboard` 는 실패할 수 있다** (사생활 모드·권한). 스쿼드 코드는 복사 실패하면 화면에 그대로 띄운다.
31. **보호명이 다른 실존 선수의 이름이 될 수 있다.** 성씨만 바꾸면 하필 명단에 있는 이름이 나온다(3건). 원본 이름 전체를 금지 집합으로 놓고 피해야 한다 — `build_cards.py` 가 그렇게 한다.
32. **급여 상한 조건 2 는 포지션을 가리면 안 된다.** 포지션 몫으로 재면 포메이션에 따라 그 몫을 벗어나 상한을 넘는다 (강원 4-3-3 이 231 > 230).
33. **원본 데이터는 저장소 밖에 둔다.** `build_cards.py` 가 경로를 인자로 받고, 실명이 0건인지 확인하고 나서야 파일을 쓴다.

34. **소리 크기는 파형으로 잰다.** 귀로 맞추면 사람마다 다르다. AnalyserNode 를 마스터에 붙여 RMS 최고값을 재면
    "골대가 다른 소리의 3배" 같은 것이 바로 보인다. 계측값은 CHANGELOG 에 남겼다.
35. **AudioContext 는 첫 사용자 입력 전에는 소리를 안 낸다** (브라우저 자동재생 정책). 그 전 이벤트는 그냥 버린다.
36. **테스트 모드는 `?test=1` 로 가린다.** 코드를 지우거나 플래그를 뒤지지 않아도 되고, 배포에서 링크를 안 걸면 그것으로 끝이다.

---

## 중요 파일 맵

| 경로 | 역할 |
|---|---|
| `docs/DESIGN.md` | **설계서 정본** |
| `docs/DECISIONS.md` | 결정 · **5장 = 사용자 지적 규칙 4건과 단계 7 계측** |
| `docs/PREP.md` | 사용자가 직접 할 것 (A-1 허락 · B-1 다른 회선) |
| `src/core/state.ts` | 상수·타입 (Phase · Restart · PendingCall · SubOrder) |
| `src/core/ball.ts` | 소유·태클·**파울**·패스·**스로인(손)**·슛·**GK 세이브/펀트**·**오프사이드 표시** |
| `src/core/rules.ts` | 킥오프·아웃·**프리킥·PK·카드·교체**·**세트피스 위치 강제** |
| `src/core/ai.ts` | 팀 AI · **압박 슬라이더** · 라인 지키기 |
| `src/core/sim.ts` | createState / step / hashState / snapshot · 사람 입력 해석 |
| `src/cards/cards.ts` · `squad.ts` · `squadcode.ts` | 6스탯·OVR·급여 / 규칙·상한·팀컬러 / 코드 |
| `src/data/cards.json` | **카드 1,024장** — `tools/build_cards.py` 가 굽는다. 손으로 고치지 않는다 |
| `src/data/pool.ts` | `cards.json` 을 풀어 `POOL`·`CLUBS` 로 |
| `tools/build_cards.py` | 원본 명단 → 카드. **보호명·구단명 표(`CLUB_NAME`)가 여기 있다** |
| `src/render3d/kits.ts` | 구단 색 → 유니폼. 색이 가까우면 원정 흰 상의 |
| `src/audio/sfx.ts` | **소리 전부** — 관중석·휘슬·킥·골·로비 배경음. 음원 파일 없음 |
| `src/render/keyview.ts` | 키 입력 표시 (테스트 모드) |
| `src/net/room.ts` · `lockstep.ts` | Trystero 로비·방 / 2인 락스텝 |
| `src/game/session.ts` | 경기 루프 (혼자 하기 + 온라인) · 메뉴 · 교체 화면 · 결과 |
| `src/render3d/referee3d.ts` | **심판 3명** (렌더 전용) |
| `src/render3d/camera.ts` | 방송 카메라 수치 — 카메라를 만지려면 여기 |
| `src/ui/squad.ts` · `waitroom.ts` · `lobby.ts` | 스쿼드 화면 / 대기실 / 로비 |
| `tools/verify.ts` | **단계 7 검증** — 지문·짝 비교·효과 크기·대칭·결정론 |
| `tools/balance.ts` · `oneone.ts` · `cards.ts` | 봇전 계측 / 1대1 전환율 / 카드·상한 |

### 재현 명령

```bash
npm install
npm test               # 81개
npm run build
npm run dev            # http://localhost:5175/KLO26/
npm run verify 200     # 단계 7 검증 (몇 분 걸린다)
npm run balance -- 120
npm run oneone 400 14  # 1대1 전환율
npm run cards          # 카드 OVR 보정 · 급여 상한
npm run clubs 90       # 실제 구단끼리 계측
npm run build:cards    # 원본 명단 → src/data/cards.json (원본은 저장소 밖)
```

브라우저 콘솔에서 `__klo.state()` · `__klo.hashes()` · `__klo.net()` · `__klo.snd()`.
개발 서버에서는 `__sfx` 로 소리를 직접 만질 수 있다 (배포 번들에는 없다).

**테스트 모드**: `http://localhost:5175/KLO26/?test=1` — 로비에 보라색 타일이 생긴다 (AI 대 AI 관전 · 키 입력 표시).

---

> 다음 세션 시작 시 다음 입력으로 컨텍스트 복원:
> `@HANDOVER.md 읽고 이어서 작업 진행`
