# CHANGELOG

## 2026-09-10 — v0.8.0 사용자 제보 11건 (DECISIONS 9장)

- **골키퍼 순간이동 제거** — 잡기는 손 거리(서서 0.8 · 날면서 1.0 m)에서만. 옆으로 지나는 공은 반응 뒤 그 경로로 **몸을 날린다**(속도 있는 다이브, 앞 12틱 비행). 못 닿으면 들어간다. **백패스 룰** — 아군이 발로 준 공은 손으로 못 잡는다.
- **방향 표시** — 방향키를 누르면 조작 선수 앞에 `›`. 공격 노랑 · 슛 모으면 붉고 길어짐 · 수비 흰색.
- **킥오프는 아군에게 짧은 패스로만** (D 를 눌러도). 차는 순간 휘슬 + 자막 "전반/후반 킥오프".
- **수비 키** — D 홀드 = 공을 향해 자동 추격(방향키로 옆 조정) · Space = 공 쪽 돌진 태클 · A = 방향키 없으면 공 쪽 슬라이딩 · **W = 골키퍼 돌진** · C 견제는 마주 보고 들이받는 드리블 ×1.5.
- **슛 보조** — 슛은 언제나 골문을 겨눈다. 방향키 위/아래 = 코너, 안 누르면 GK 가 비운 코너. 뒤로 밀고 차면 오차만 ×1.6.
- **추가시간** — 죽어 있던 시간을 모아 1~5분 발표(`45'+2`). 추가시간 뒤 공이 죽었을 때 끝내고, 공격 중이면 최대 20초 기다린다. 하프타임 자막 "전반 종료".
- **수비 강화** — 가로채기 반경(posn)·성공(mark)·**슛 블록**(posn/tck, `block` 사건)·마크 거리(mark)·압박 예측(posn).
- **선수 모델 마르게** — 몸통·팔다리 굵기 ×0.8.
- **스쿼드 화면** — 저장 슬롯 10 → **5칸 띠**(맨 위) · 급여를 큰 숫자 + 남은 급여 · 선수 목록마다 **바꾸면 ±급여 · 상한 초과** 표시(초과는 못 고름) · **강화 상자**(설명 · OVR 지금→다음 · 자동 배분 셋).
- **로비 스쿼드 띠** — 구단 색·급여 게이지·큰 버튼. **대기실** — 양쪽 구단 색·선발 OVR·급여·팀컬러·강화.
- 문서 — `docs/플레이-가이드.md` 신설 · `docs/공지글-모음.md` 1차 문안.
- 계측(합성 40판) — 평균 3.10골 · 유효슛 9.8 · 선방 7.0 · 패스 47%. 파울 17.5/판은 많다(단계 7 후속). 테스트 90개.
- `verify 200` — 지문 4/4 · 팀컬러 +11.8%p ✅ · 강화 수비 +2.7%p(−0.3 에서) · 공격 +2.0%p(7.8 에서 ↓) △ · 능숙도 87.3% ✅ · 결정론 100/100 ✅ · 홈/원정 42.5% ⚠ → `npm run asym 100`(새 도구, 하프별 진단)에서는 51.7% — 시드 노이즈로 판단.

## 2026-09-09 — v0.7.0 원본 명단 갱신 (KM26 오늘자) — 1,024 → 1,056명

개리그 매니저(KM26)가 오늘 로스터·능력치를 고쳤다. 그것을 그대로 받아 왔다.
원본 `github.com/KleagueM2026/KM26v2.0` 커밋 `0a08e86`(01:17) · `547f422`(05:25).

### 무엇이 바뀌었나

| | 예전 | 새 |
|---|---|---|
| 선수 | 1,024명 | **1,056명** (+32) |
| 구단 | 29 (1부 12 · 2부 17) | 그대로 — 부·이름 변동 없음, **구단 색 6곳** 갱신 |
| dataHash | `6d4b32c636580eae` | `b53cd9ed65ffe691` |
| `POOL_HASH` | — | **57542 로 바뀜** |
| 급여 상한 | 238 | **252** |
| OVR 보정 | a=1.6794 b=−49.02 | a=1.6236 b=−44.00 |
| OVR 최대 | 95 | 91 |

명단은 29개 구단 전부에서 조금씩 움직였다 (김천 +6 · FC서울 +5 · 천안 +4 · 전북 +4 …
안양 −2 · 김포 −1 …). 어느 구단도 18명 아래로 내려가지 않았다.

**저장된 스쿼드는 전부 버려진다.** `POOL_HASH` 가 바뀌었기 때문이고, 일부러 그렇게 만든 장치다 —
저장 스쿼드는 선수 **id** 만 갖고 있어서, 명단이 바뀌면 같은 id 가 딴 선수를 가리킨다
(v0.6.0 의 "골키퍼 자리에 공격수" 사고가 바로 그것이었다). 돌아다니던 스쿼드 **코드**도 같다.

### 급여 상한·OVR 보정은 손대지 않았다

둘 다 카드 풀에서 **런타임에** 다시 뽑는다 (`calibrateOvr` · `computeCap`). 상한이 238 → 252 로
움직인 것도 코드를 고쳐서가 아니라 데이터가 바뀌어서다. 29개 구단 전부 자기 선수 18명으로
상한 안에 든다 (가장 빠듯한 FC 서울 244/252, 가장 여유로운 파주 64/252).

### 카드 장수를 더 이상 못 박지 않는다

`1024` 를 코드·테스트·화면에 박아 두었더니 이번에 전부 걸렸다. 원본 명단은 앞으로도 움직인다.
- 테스트: 장수 대신 **깨지면 안 되는 것**을 본다 — 구단 29 · 구단마다 18명 이상 + GK 1명 이상 ·
  id 중복 없음 · id 가 11비트(스쿼드 코드 규격) 안.
- 화면: 로비·스쿼드의 "1,024장" 을 `POOL_SIZE` 에서 읽는다.

### 추출 파이프라인 (KMD26 도구)에서 걸린 것 둘

1. **패치 GK-01 이 0곳** — 원작자가 `assignPrefPos` 에 같은 고침을 위에서 넣었다
   (골키퍼는 GK, 표에 GK 가 남아 있으면 무시). KMD26 패치와 의미가 같아 뺐다.
2. **`UID_GEN is not defined`** — 새 `nextGenUid` 가 그 상수를 본다. 그런데 선언이
   `const UID_CLUB=1001, UID_COACH=2001, UID_REAL=10000, UID_GEN=900000;` 한 줄에 얹혀 있고
   `jsclosure.collect_decls` 는 다중 선언의 **첫 이름만** 색인한다. 뿌리에 `UID_CLUB` 을 넣어 해결.

되풀이할 수 있게 HANDOVER 에 파이프라인 전체를 적어 두었다.

### 계측 — 무엇이 실제로 데이터를 타는가

`npm run verify` · `oneone` · `balance` 는 **합성 스쿼드**(`synthSquad`)끼리 붙인다. **엔진을 재는
것이지 카드 데이터를 재는 게 아니다.** 명단을 갈고 다시 재도 소수점까지 같은 값이 나왔다
(팀컬러 +10.0%p · 홈/원정 51.5% · 능숙도 82.8% · 1대1 38.8% · 결정론 100/100).
카드 데이터를 실제로 타는 것은 `npm run clubs` · `npm run cards` · 테스트뿐이다.

| 실제 구단 계측 | 예전 | 새 |
|---|---|---|
| 90판 평균 골 | 3.10 | 3.56 |
| 1부 vs 2부 승률 | 60.2% | **70.5%** |

1부/2부 격차가 벌어진 것은 원본이 그렇기 때문이다 (FC서울 급여 244 vs 파주 64). 버그가 아니다.

덧붙여, `verify` 를 기본 표본(120)으로 돌리면 홈/원정 56.7% · 수비 강화 −7.1%p 가 나오는데
둘 다 노이즈다. 시드가 `5000+i` 고정이라 N 이 바뀌면 표본 자체가 바뀐다 — **항상 200 으로 잰다.**

## 2026-09-09 — v0.6.1 스쿼드 수정 (선수 · 자리 · 선발↔벤치)

구단을 다시 고르지 않고 **바꾸고 싶은 것만 바꾸는** 흐름을 붙였다 (사용자 요청).
바꾸는 세 가지가 전부 원래도 됐지만, 어디를 눌러야 하는지가 보이지 않았다.

- **로비 버튼을 둘로** — "스쿼드 수정"(전술판으로 바로) · "구단 바꾸기"(구단 고르기로).
  저장된 스쿼드가 있으면 수정으로 열리고, 없으면 어느 쪽을 눌러도 구단 고르기로 간다.
- **바꿀 수 있는 자리를 보여 준다** — 자리를 고르면 맞바꿀 수 있는 자리에 **초록 테두리**,
  안 되는 자리는 흐려진다. 골키퍼를 고르면 벤치 골키퍼 한 명만 초록이 된다.
- **선발↔벤치 한 번에** — 오른쪽 카드에 "벤치로 내리기 / 선발로 올리기" 버튼.
  짝은 자동으로 고른다. 선발을 고르면 **그 자리 능숙도×2 + OVR** 이 가장 높은 벤치 선수,
  벤치를 고르면 **능숙도가 높고 지금 앉은 사람이 약한** 선발 자리다. 짝이 없으면 버튼이 꺼진다.
- **전술판 아래 한 줄 안내** — 자리 옮기기 · 선발↔벤치 · 선수 교체 셋을 적어 뒀다.
- 구단 고르기에서 **"그만두고 스쿼드 수정으로"** — 실수로 들어왔을 때 스쿼드를 지키고 돌아간다.

## 2026-09-09 — v0.6.0 스쿼드 화면 개편 (구단 먼저 · 전술판) + 골키퍼 자리 버그

### 버그 — 골키퍼 자리에 공격수가 앉아 있었다 (사용자 제보)
저장된 스쿼드는 선수 **id** 만 갖고 있는데, 카드 데이터를 자리표시자에서 실제 명단으로 바꾸면서
같은 id 가 딴 선수를 가리키게 됐다. `loadSquad` 가 길이와 포메이션만 보고 그대로 받아들여
골키퍼 자리에 공격수가 앉았다.
- 저장 스쿼드에 **데이터 지문(`hash`)** 을 같이 넣고, 다르면 버린다. 저장 슬롯 10칸도 같다.
  (스쿼드 **코드**에는 원래 같은 지문이 박혀 있었다 — 저장만 빠져 있었다, DESIGN 5.9)
- 읽을 때 `checkSquad` 도 돌린다. 규칙을 어기면 버리고 다시 짜게 한다.
- **골키퍼 자리(선발 0번)에는 골키퍼만** 설 수 있게 막았다. 필드 자리에는 골키퍼를 못 넣는다(벤치는 자유).
  전술판에서 자리를 맞바꿀 때도 같은 규칙이라 조작으로 깨뜨릴 수 없다.

### 스쿼드 화면 개편 (사용자 요청 — 개리그 매니저·듀얼 참고)
1. **구단을 먼저 고른다** — 29개 구단을 구단 색·부·평균 OVR·최고 선수와 함께 보여 준다.
2. 고르면 **그 구단 선수로 선발 11 + 벤치 7 을 자동으로 채운다** (팀컬러 +4).
3. **전술판** — 초록 피치에 센터 서클·페널티 박스·골 에어리어·골대를 그리고 포메이션 좌표(`SLOT_XY`,
   KM26·KMD26 과 같은 값)대로 선수를 앉힌다. 자리를 눌러 고른 뒤 다른 자리를 누르면 **서로 바뀐다**.
   끌어다 놓아도 된다. 카드 왼쪽 띠 색이 **그 자리 능숙도**다.
4. 선수 목록은 기본이 **내 구단 선수**다. "전 구단 1,024장" 으로 넓힐 수 있다(팀컬러는 깨진다).
   자리를 고르면 그 자리 **능숙도가 높은 선수가 위로** 오고, 넣을 수 없는 선수는 흐려진다.
5. "자동 채우기"로 언제든 그 구단 최적 스쿼드로 되돌린다.

## 2026-09-09 — v0.5.0 소리 · 테스트 모드 · 로비 개편

### 소리 (DESIGN 7.3) — 파일 없이 전부 코드로
샘플·음원을 하나도 쓰지 않는다. 오실레이터 + 노이즈 + 필터뿐이라 **저작권 문제가 없다** (`src/audio/sfx.ts`).
- **관중석** — 필터 건 잡음이 계속 흐르고, 공이 골문에 가까울수록 두꺼워진다. 소리만 듣고도 위험한 줄 안다.
- **휘슬** — 2.6·3.3 kHz 두 음을 42 Hz 로 떨어 만든 트릴. 파울·킥오프는 한 번, 하프·종료는 세 번(마지막이 길다).
- 킥(파워 반영) · 스로인(손이라 부드럽다) · 태클 · 선방 · 골대(금속 배음) · 골(함성) · 경고/퇴장 · 화면 조작음.
- **로비 배경음** — 느린 화음 패드 + 아르페지오. 4마디 진행을 오실레이터로 만든다. 경기에 들어가면 끄고 관중석만 남긴다.
- 소리 위치는 공 x 로 좌우 팬. 음소거는 경기 화면 버튼·Esc 메뉴·설정 어디서나.
- **크기를 계측으로 맞췄다** — 파형 RMS 로 재서 골 0.051 · 휘슬 0.045 · 태클 0.038 · 골대 0.031 · 슛 0.025 · 스로인 0.017
  (관중석 바닥 0.004). 처음엔 골대가 0.111 로 튀고 스로인이 0.0035 로 안 들렸다.
- 골대를 맞으면 `post` 이벤트를 남긴다 (물리는 그대로 — 결정론에 영향 없음).

### 테스트 모드 (사용자 요청) — 배포에서는 안 보인다
주소에 **`?test=1`** 을 붙였을 때만 로비에 보라색 타일이 뜬다. 링크를 안 걸면 그것으로 가려진다.
- **AI 대 AI 관전** — 두 팀 다 봇. 홈·원정 난이도를 따로 줄 수 있다.
- **키 입력 표시** (`src/render/keyview.ts`) — 지금 눌린 물리 키와 sim 으로 나가는 `Input` 비트를 화면에 띄운다.
  키가 안 먹는지 · 조합(Q+D)이 잡히는지 · 공수에 따라 뜻이 바뀌는지를 눈으로 본다.

### 로비 개편 (사용자 요청)
혼자 하기 · 방 만들기 · 설정을 **큰 버튼 셋**으로 빼고, 누르면 **팝업**이 뜬다 (bedorage-duck 방식).
맨 위에 내 스쿼드 한 줄 + 스쿼드 짜기 버튼, 아래에 방 목록. Esc·바깥 클릭으로 팝업이 닫힌다.

## 2026-09-09 — v0.4.0 단계 1 · 실제 선수 데이터 (보호명)

원작자 허락을 받아 (PREP A-1) K리그 2026 **29개 구단 1,024명**을 넣었다. 자리표시자("가1-07")는 사라졌다.

- `tools/build_cards.py` — 원본 명단 → `src/data/cards.json`. 하는 일 셋:
  · **보호명** — KM26 `fictName` 그대로. 한국 선수는 성씨를 다른 실재 성씨로(김→구, 이→여 …),
    외국 선수는 첫 글자 초성을 민다. 결과가 **다른 실존 선수의 이름**이 되면 마지막 글자를 밀어 피한다(3건).
  · **구단명은 지역명만** — 기업 이름·구단 고유 별칭을 쓰지 않는다 (DESIGN 10.2). 표 한 곳(`CLUB_NAME`)만 고치면 된다.
  · **컬럼 형식**으로 굽는다 — 키를 1,024번 되풀이하지 않아 700 KB → 174 KB.
  끝에서 **실명이 한 건도 안 남았는지 직접 확인**하고 나서야 파일을 쓴다.
- `src/data/pool.ts` — 생성기가 아니라 `cards.json` 을 푼다. 구단 색·부(1/2)도 함께.
- **구단 스쿼드** — `clubSquad()` 로 한 구단 선수만 뽑아 팀컬러 +4 를 받는 순수 팀을 짤 수 있다.
  스쿼드 화면의 "구단 통째로 불러오기", 로비의 "상대 구단" 선택, 처음 여는 사람의 기본 스쿼드가 모두 이걸 쓴다.
- **유니폼이 구단 색** (`src/render3d/kits.ts`) — 상의 `col` · 소매/바지 `col2`.
  두 팀 색이 130 안쪽이면 원정이 흰 상의로 갈아입는다(조합의 36%). 전광판 글씨·레이더 점 색도 따라 바뀐다.
- 계측 — 실제 구단 90판 평균 **3.10골** (목표 2.5~3.5) · 0:0 3% · **1부가 2부를 60.2%** 로 이긴다 · 같은 부 홈 승률 48.8%.
- **급여 상한 버그를 계측이 잡았다** — 조건 2 를 포지션 몫(GK2/DF6/MF6/FW4)으로 재고 있어서
  4-3-3 처럼 공격수가 많은 포메이션이 상한을 넘었다(강원 231 > 230). 포지션을 가리지 않은 최고 18명으로 바꿔 **238**.
  이제 29개 구단 전부 자기 선수만으로 규칙을 통과한다 (테스트가 확인).
- 테스트 65 → **81개** (`tests/pool.test.ts` 16개 — 실명 없음·구단명에 기업명 없음·능력치 완전성·구단 스쿼드).

## 2026-09-09 — v0.3.0 단계 4~7 (규칙 · 카드/스쿼드 · 온라인 대전 · 밸런스 계측)

### 사용자가 지적한 축구 규칙 4건
- **킥오프 위치** — 전원 자기 진영, 차지 않는 팀은 센터 서클 밖. 매 틱 강제해서 조작으로도 못 어긴다.
- **골키퍼 보호** — 손에 든 공은 슬라이딩·압박으로 못 뺏는다. 도전 자체가 파울(골키퍼 차징) + 경고 확률.
- **골키퍼 롱킥** — `gkPunt` (55~72 m). 골킥에서 D 는 슛이 아니라 길게 차기.
- **스로인은 손으로** — 최대 14 m/s · 반드시 뜬다 · 직접 골 무효 · 던진 사람 두 번 터치 반칙.
- **심판 3명** — 주심(대각선 추적 · 카드) + 부심 2명(오프사이드 라인 · 깃발). 렌더 전용이라 결정론에 끼지 않는다.

### 단계 4 — 규칙
오프사이드(패스 순간 표시 → 관여하면 판정 · 스로인/골킥/코너 제외) · 파울 · 경고/퇴장(두 번째 경고) ·
직접 프리킥 · PK · 교체 3명/벤치 7명(다음 데드볼 적용) · 세트피스 위치를 매 틱 강제(벽 9.15 m · 스로인 2 m · 골킥은 박스 밖).
페이스 컨트롤을 **Ctrl → Shift** 로 (사용자 결정 — Ctrl+W 가 크롬 탭을 닫는다).

### 단계 5 — 카드 · 스쿼드
6스탯 · OVR(앵커 보정) · 급여표 · 급여 상한(도구가 데이터로: 265) · 팀컬러 · 강화 예산 24 ·
스쿼드 코드 `KLO26-…`(339비트 · 체크섬 10비트) · 스쿼드 화면 · 저장 슬롯 10.
카드 1,024장은 **자리표시자** — 실제 K리그 데이터는 허락 확인(PREP A-1) 뒤에.

### 단계 6 — 온라인 대전
Trystero 로비·방(정원 2) · 2인 락스텝(지연은 RTT 로) · 60틱 해시 대조 · 어긋나면 방장이 스냅샷 리싱크 ·
대기실(서로 스쿼드 코드를 주고받고 **받는 쪽이 다시 검사**) · 상대 이탈 시 그 시점 스코어로 종료.
로컬 두 탭 실측: 약 8,000틱 동안 양쪽 해시 전부 일치 · 리싱크 0 · 끊김 0. 일부러 어긋뜨리자 잡아내 되돌렸다.

### 단계 7 — 계측이 잡아낸 것
- **압박 슬라이더가 연결돼 있지 않았다.** 지문 검사 5시드 중 1시드만 달라졌다 → 붙는 거리·압박 인원·태클 시도를 함께 움직이게 고쳐 5/5.
- **멘탈리티가 지표를 안 움직였다** (38%) → 앵커 ±4→±6 m, 전진 패스 가중 → 슛 지표 64%.
- **오프사이드 판당 11.5회** → AI 가 라인을 지키고 오프사이드 위치로는 패스하지 않게 → 1.0회.
- **1대1 인데 골이 안 들어갔다** (사용자 제보) → 사람 슛이 정중앙(=골키퍼)을 겨누던 것 + 골키퍼가 반경 안이면 무조건 막던 것.
  이제 1대1 14 m 전환율 **39%**, 8 m **58%**.
- 봇전 120판: 평균 **2.72골** (목표 2.5~3.5) · 0:0 6% · 홈/원정 대칭 51.5% · 결정론 100/100 · 능숙도 초록 82.8%.
- 팀컬러 +4 는 승률 **+10.0%p** (목표 8~15%p). 강화 24 는 **공격수에 주면 +7.8%p, 수비수에 주면 0%p** — 어디에 쓰느냐가 갈린다.

테스트 26 → **64개**. 도구 `tools/verify.ts`(단계 7 검증) · `tools/oneone.ts`(1대1) · `tools/cards.ts` 추가.

## 2026-09-09 — v0.2.0 단계 3 · 조작·렌더·혼자 하기

- **브라우저에서 봇과 한 판이 된다.** 로비 → 혼자 하기(난이도 3단계 · 전후반 2/3/4분 · 포메이션 12종) → 경기 → 결과.
- `src/render3d/` — Three.js **방송 카메라**(사이드라인 바깥 38 m · 높이 30 m · 공 리드 6 m · 골문 앞 줌인 FOV 32→26 · 좁은 창은 세로 시야를 늘려 좌우를 자르지 않음) · **찰흙 선수**(프리미티브 조립 + `MeshToonMaterial` 3단 · 키 `h/180` · 굵기 `sqrt(w/75)` · 머리 4종 id 해시 · 등번호 캔버스 · 피부색 한 가지) · 코드 애니메이션(달리기·킥·슬라이딩·넘어짐·GK 다이브·GK 볼 들기) · 피치(줄무늬·라인 캔버스 텍스처) · 골대·네트 · 관중석 · 공 그림자 데칼 · 조작 표시(화살표·발밑 링·이름표).
- `src/game/` — `ticker.ts`(bedorage-duck Worker 타이머 그대로) · `localInput.ts`(**DESIGN 3.1 표준 키 배치 전부** — `e.code` 로 읽어 한글 IME 무관) · `session.ts`(60Hz 틱 + rAF 보간 · Esc 메뉴 · 결과표 · `beforeunload` 로 Ctrl+W 오작동 방지).
- `src/render/` — HUD DOM 오버레이(전광판·시계·단계 배너·조작 선수 카드·체력·파워 게이지·전술 프리셋·키 힌트) · 레이더.
- `src/ui/` — 로비 · 설정(그림자·해상도 75%·키 힌트, localStorage) · **폰 차단**(`maxTouchPoints`+`pointer: fine`, 폭 < 900px).
- 코어 — **사람 킥커의 리스타트 킥 방향** (`performRestartKick(st, aim)`: 방향키 + S/W/A/D, 없으면 AI 선택). 결정론 그대로.
- 테스트 26개 — 키 매핑 · 카메라 수학 · 리스타트 킥(사람 입력 결정론 포함) 추가.
- 확인한 것 — 스크립트로 흘려 넣은 키 입력으로 사람 팀이 **골을 넣었다**(40초 · 슛 4 · 골 1). Esc 메뉴 · 하프타임 진영 교체 · 종료 결과표 동작. 렌더 드로우콜 약 740(그림자 포함) · 삼각형 13만.
- 못 잰 것 — **60fps** 는 Claude 브라우저 패널이 뒤에 있으면 rAF 가 멈춰 못 쟀다. 사용자 PC 에서 우상단 fps 로 볼 것.
- 소리는 아직 없다 (단계 3 기준에 없어 뺐다).

## 2026-09-08 — v0.1.0 단계 2 · 시뮬 코어

- `src/core/` 9 파일 — 피치·공·선수 물리, 소유(붙임 모델)·태클·슬라이딩·패스(땅볼/스루/로빙/크로스)·슛(파워·칩)·걷어내기·GK(잡기/펀칭/배급), 팀 AI(앵커·지원 러닝·압박/커버/마크·볼 소유 효용·요격), 규칙(시계·하프·킥오프·골·스로인/골킥/코너), 사람 입력 해석(3.1 키 전부), 해시·스냅샷. 합성 스쿼드로 검증.
- 실측 — 같은 시드 해시 100% 일치 · 틱당 0.017 ms · 20판 평균 4.4골 · 슛 16 (유효 79%) · 패스 성공 40%. 골·유효슛은 단계 7 에서 조정.
- 겪은 것 — 빠른 공이 발을 뚫고 지나가던 것(선분 거리·요격 지점으로 해결), AI 가 잡자마자 되차던 것(20틱 보유).
- `npm run balance` · `npm run probe` · 테스트 10개.

## 2026-09-08 — v0.0.1 설계

- 설계서 `docs/DESIGN.md` v0.1 (13장) — 정체성·규칙·조작·결정론 시뮬·카드/스쿼드·네트워크·화면·구조·배포·비용/법적·개발 단계·검증·백로그.
- `docs/DECISIONS.md` — 사용자 결정 3건(직접 조작 · 전원 개방+급여 상한 · KLO26) · 기본안 17건 · 확인 필요 5건.
- `docs/PREP.md` — 원작자 허락 재확인 · 저장소 · Pages · 기기 · 비용 원칙.
- README · HANDOVER · 공지글 틀.
- 스캐폴드 — Vite + TypeScript + vitest + GitHub Pages 워크플로. `src/core/rng.ts`·`fixedmath.ts` (bedorage-duck 복사) + 테스트. 게임 코드 없음.
- 추가 결정 5건 반영 — **선수 이름 보호명**(빌드 단계 적용, 저장소에 실명 0건) · 경기 기본 3분 · **키보드만 검증**(패드 미검증) · 급여 상한 조건에 **K리그2 포함** · 공개 채널은 공개 시점에.
- 시점·렌더를 **Three.js 방송 카메라**로 (사용자 결정) — 찰흙 선수 · 키 `h/180` 반영 · 피부색 안 함(데이터 없음) · 닮게 그리지 않음. DESIGN 7.1 다시 씀, `package.json` 에 `three`.
- **PC 전용** (사용자 결정) — 터치 조작 없음, 폰·태블릿은 안내 화면만.
- **키 배치를 축구 게임 표준 키 그대로** (사용자 결정) — 방향키 · S/W/A/D/E · Space · C · Q · Ctrl · 조합키 · `[ ]`. 입력 비트도 키 이름으로. **게임패드는 계획에서 제외** — 백로그에도 없음, 첫 화면·README 에 명시.
- **제3자를 거명하는 고지 문구 전부 삭제** (사용자 결정) — NOTICE.md 삭제, README 고지 절 삭제, 첫 화면 문구 축소, 타 게임·회사 이름을 문서 전체에서 제거.
- 저장소 `goormigrm/KLO26` 첫 커밋·푸시. Pages(Actions) 켜고 첫 배포 — <https://goormigrm.github.io/KLO26/>.
