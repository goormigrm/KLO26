# HANDOVER — 2026-09-10 (5차)

> **이 문서가 최신입니다.** 단계 1~7 이 전부 들어가 있고, 2026-09-10 에 **사용자가 직접 해 보고 낸 제보 11건**을 반영했습니다
> (골키퍼 순간이동 · 방향 표시 · 킥오프 규칙 · 수비 키 · 슛 보조 · 추가시간 · 수비 강화 · 마른 선수 · 문서 · 스쿼드/대기실 UI · 강화 설명 —
> DECISIONS 9장). 남은 것은 다른 회선 두 대 실제 대전(PREP B-1)과 다시 한 판 해 본 뒤의 조작감입니다.

## 세션 메타데이터

| 항목 | 값 |
|---|---|
| 날짜 | 2026-09-10 (직전 4차: 2026-09-09) |
| 작업 디렉터리 | 노트북: `C:\Users\tkdrm\Workspace\personal\KLO26` · 메인 PC: `C:\Users\tkdrm\OneDrive\Desktop\klo26` |
| 저장소 | `https://github.com/goormigrm/KLO26` · `main` |
| 배포 | <https://goormigrm.github.io/KLO26/> — push 하면 Actions 가 테스트·빌드·배포 |
| 참고한 곳 | 배도라지덕(bedorage-duck) — 이 PC 에서는 `C:\Users\tkdrm\OneDrive\Desktop\철FPS`. KM26·KMD26 은 `Desktop/km26` · `Desktop/KMD26v1.0` |

두 PC 를 오가므로 다음 세션은 `git fetch origin && git status -sb` 부터.

---

## 지금 되는 것

| 단계 | 무엇 | 상태 |
|---|---|---|
| 2 | 결정론 시뮬 코어 (물리·AI·규칙) | ✅ |
| 3 | Three.js 방송 카메라 · 찰흙 선수 · 표준 키보드 · 폰 차단 · 혼자 하기 | ✅ |
| 4 | 오프사이드 · 파울/카드 · 프리킥/PK · 교체 · 세트피스 위치 강제 · **심판 3명** | ✅ |
| 5 | 카드 6스탯·OVR·급여 상한·팀컬러·강화 예산·스쿼드 코드·스쿼드 화면·저장 슬롯 | ✅ |
| 6 | Trystero 로비·방 · 2인 락스텝 · 해시/리싱크 · 대기실 | ✅ (로컬 두 탭 검증) |
| 7 | 밸런스 계측 (`npm run verify`) · 슬라이더 지문 · 짝 비교 · 효과 크기 | ✅ |
| **1** | 실제 K리그 2026 선수 데이터 (보호명) | ✅ **원작자 허락 받음 (2026-09-09)** |
| — | 소리 (Web Audio 절차 생성 — 음원 파일 없음) | ✅ |
| — | **테스트 모드** (`?test=1`) — AI 대 AI 관전 · 키 입력 표시 | ✅ |

**흐름**: 로비 → (스쿼드 수정 | 구단 바꾸기) → (혼자 하기 | 방 만들기/참가 → 대기실) → 경기 → 결과.

---

## 이번 세션 (2026-09-10) — 사용자 제보 11건

정본은 **DECISIONS 9장 F-1~F-11** (제보 → 어떻게 했나 → 어디). 여기엔 다음 사람이 알아야 할 것만.

| 무엇 | 핵심 |
|---|---|
| **GK 잡기 모델** | 잡기는 손 거리(서서 0.8 · 날면서 1.0 m)에서만. 경로가 손 밖이면 반응 뒤 `ACT_DIVE` + 속도로 **실제로 날아간다**(앞 12틱 비행, `sim.ts`). 못 닿으면 골. `tests/feedback.test.ts` 가 "공 한 틱 0.6 m 이하" 로 순간이동을 잡는다 |
| **슛 보조** | `doShoot` 사람 분기에 `straight`(빗나감) 경로가 없다. 방향키 y 성분 = 코너, 없으면 GK 가 비운 코너. 뒤로 밀면 σ×1.6 |
| **킥오프** | `performRestartKick` 맨 앞에서 `kickoffTarget` — 자기 진영 아군에게 땅볼만. `whistle` 사건 + `callText '전반/후반 킥오프'`. AI 킥오프 분기는 지웠다 |
| **추가시간** | `advanceClock`: `stoppage`(데드볼 초) → 정규 종료 때 `added`(1~5분) 발표 → `halfSec + added×4초` 뒤 **공이 죽었을 때** 끝. 공격 중이면 20초 유예. `switchSides` 가 둘 다 리셋. HUD 시계 `45'+2` 는 `addedMinute` |
| **수비 키** | `sim.ts` step: 조작 선수가 `press`/`tackleT` 면 공 쪽으로 자동 이동(방향키 45% 블렌드). `handleInput`: A 방향키 없으면 공 쪽 슬라이딩 · W = `team.gkRush` · C = `faceBall`. `contestBall` 견제 ×1.5 |
| **수비 강화** | `tryControl` 가로채기 반경 `+0.28·posn` · 성공 `+0.15·mark`; `doShoot` **블록**(`block` 사건); `aiDecide` 마크 거리/압박 예측. 효과 크기는 아래 검증 결과 |
| **방향 표시** | `renderer3d.ts` `chev` 그룹 — `team.inX/inY` 를 읽는다(렌더 전용). 색: 노랑/붉음(슛 파워)/흰색 |
| **스쿼드 UI** | 저장 슬롯 10 → 5, 띠(`slotsStrip`)로 맨 위. 옛 `drawSlots` 페이지 삭제. 급여 큰 숫자·남은 급여·목록의 ±급여·상한 초과 잠금. 강화 상자 + `autoEnh('even'|'attack'|'reset')` |
| **대기실·로비** | `waitroom.sideHtml` — 구단 색 띠·선발 OVR·급여·팀컬러·강화. 로비 `squad-bar v2` |
| **테스트** | 83 → **90** (`tests/feedback.test.ts` 7개). `restart.test` 의 "D 는 길게 찬다" 를 새 규칙(짧은 패스)으로 바꿨다 |

**같은 날 두 번째 요청 6건 (DECISIONS 10장 G-1~G-6)** — 자동 채우기 선발/후보 + 기준(`autoFill`) · **별점 표시**(`cards.ts ovrStars/statStars`, `ui/stars.ts starHtml/miniBars` — 숫자는 급여·능숙도만) · 목록 머리줄 · **JSON 저장/불러오기**(코드 UI 제거, `squadcode` 는 대기실 내부용) · **저장 안 하고 돌아가기**(`original` 스냅샷, `cancel`) · **유니폼 단색/원정 흰색**(`kits.ts` — `col2` 는 이제 아무 데도 안 쓴다).

**아직 안 한 것 / 봐야 할 것**
- **수비 강화는 +2.7%p 까지 왔다** (목표 +4). 다음 손잡이 후보: 블록 반경/성공률 계수, 가로채기 `0.28·posn`. `npm run verify 200` 은 20분 넘게 걸리니 효과 크기만 따로 재는 옵션을 만들면 좋다.
- **강화 효과가 둘 다 작다** (수비 +2.7 · 공격 +2.0%p, 목표 8~15). 수비를 세게 만들며 공격 강화의 효과까지 눌렸다. "강화가 값을 하게"가 다음 밸런스의 첫 항목 — 강화 +1 이 능력치 40종 +1 인데 경기 스킬 곱셈 안에서 묻힌다. 강화분을 스킬에 직접(예: ×1.02/레벨) 얹는 방법을 재 볼 것.
- 홈/원정 42.5% 는 시드 노이즈로 판단했다(위 표). `npm run asym 200` 으로 한 번 더 재서 47~53% 안이면 닫는다.
- 파울 17.5/판(합성 40판)은 많다 — Space 돌진 태클이 파울을 늘렸을 수 있다. `npm run balance -- 120` 으로 다시 재고 `foulP` 계수를 본다.
- 방향 표시 `›` 의 크기·색은 사람이 봐야 한다. 골키퍼 다이브가 "날아가는" 것으로 보이는지도.
- 파워 슛(F+D+D)·감아차기는 여전히 없다 (DESIGN 13장).

**스쿼드 화면 두 갈래** (`src/ui/squad.ts`, `mode: 'club' | 'board'`)
- `SquadScreen(host, onDone, 'edit')` — 저장된 스쿼드가 있으면 **전술판**부터. 로비 "스쿼드 수정".
- `SquadScreen(host, onDone, 'club')` — **구단 고르기**부터. 로비 "구단 바꾸기".
- 저장된 스쿼드가 없으면 `'edit'` 로 들어와도 구단 고르기로 떨어진다 (`loadSquad()` 가 `null`).
- 전술판에서 바꾸는 세 가지: 자리↔자리(두 번 클릭/드래그) · 선발↔벤치(`#quick-swap`) · 선수 교체(오른쪽 목록).
  `mark(i)` 가 `can`/`cant` 클래스를 붙여 **바꿀 수 있는 자리를 초록으로** 보여 준다.
  `bestPartner(i)` 가 선발↔벤치 짝을 자동으로 고른다. 두 곳 다 `canPlace()` 하나만 본다 —
  규칙(골키퍼 자리)을 고치려면 `canPlace()` 만 고치면 된다.

---

## 이번 세션에서 한 것

### 사용자가 지적한 축구 규칙 4건 + 심판

1. **킥오프 위치** — 전원 자기 진영, 차지 않는 팀은 센터 서클 밖. `enforceRestartPositions` 가 **매 틱 강제**하므로 조작으로도 못 어긴다.
2. **골키퍼 보호** — 손에 든 공은 슬라이딩·압박으로 못 뺏는다. 도전 자체가 파울(골키퍼 차징) + 경고 확률.
3. **골키퍼 롱킥** — `gkPunt` (55~72 m, `kic` 반영). 골킥에서 D 는 슛이 아니라 길게 차기.
4. **스로인은 손으로** — `doThrow`. 최대 14 m/s · 반드시 뜬다 · **직접 골 무효** · 던진 사람 두 번 터치 반칙.
5. **심판** — 주심 1(대각선 추적 · 카드) + 부심 2(오프사이드 라인 · 깃발). `src/render3d/referee3d.ts`, **렌더 전용**이라 결정론에 끼지 않는다.

### 밸런스 (사용자 제보 "1대1 인데도 골이 안 들어간다")

원인이 둘이었다.
- **사람 슛 자동 보조가 골문 정중앙을 겨눴다** — 방향키를 옆으로 안 밀면 골키퍼가 서 있는 자리로 갔다. 이제 **골키퍼가 비운 코너**를 기본값으로 잡고 스틱 옆 성분으로 옮긴다.
- **골키퍼가 반경 2.2 m 안이면 무조건 막았다** — 이제 **반응 시간 + 몸 날릴 시간**으로 닿는 거리를 정한다.

계측: 1대1 14 m 전환율 **39%**, 8 m **58%** (`npm run oneone`). 봇전 120판 평균 **2.72골** (목표 2.5~3.5).

### 단계 7 계측이 잡아낸 것 (이 방법론이 실제로 일했다)

| 무엇 | 어떻게 드러났나 | 고친 것 |
|---|---|---|
| **압박 슬라이더가 연결돼 있지 않았다** | 지문 검사 5시드 중 1시드만 달라짐 (`d < dPress + 10` 이라 늘 참) | 붙는 거리 · 압박 인원(1~3명) · 태클 시도를 함께 움직인다 → 5/5 |
| 멘탈리티가 지표를 안 움직였다 | 짝 비교 38% | 앵커 ±4 → ±6 m · 전진 패스 가중 → 슛 지표 64% |
| 오프사이드 판당 11.5회 (실제 2~4회) | 계측 | AI 가 라인을 1.5 m 여유로 지키고, 오프사이드 위치 동료에게는 패스하지 않는다 → 1.0회 |
| 패스 성공률 44% | 계측 | 받으라고 보낸 선수의 퍼스트 터치를 올렸다 (가로채기와 같은 확률이었다) → 51% |

---

## 검증 결과

> **먼저 알아 둘 것.** `npm run verify` · `npm run oneone` · `npm run balance` 는 `synthSquad` 로
> 만든 **합성 스쿼드**끼리 붙인다 (`tools/verify.ts:21`). 즉 **엔진을 재는 것이지 카드 데이터를 재는 게
> 아니다.** 명단을 갈아도 이 숫자들은 안 움직인다 — 2026-09-09 갱신 뒤 다시 재도 소수점까지 같았다.
> **카드 데이터를 실제로 타는 계측은 `npm run clubs` · `npm run cards` · 테스트뿐이다.**

### 엔진 (`npm run verify 200` — 합성 스쿼드)

| 항목 | 결과 | 기준 |
|---|---|---|
| 슬라이더 지문 (line·press·width·mentality) | **4/4 연결됨** (9/10 재확인) | 전부 |
| 압박 ↑ → 상대 패스 성공률 ↓ | 81% → **56% △** (9/10 — 가로채기가 posn/mark 를 타면서 잡음이 커졌다) | ≥62% |
| 압박 ↑ → 파울 ↑ | 69% → 64% ✅ | |
| 멘탈리티 ↑ → 우리 슛 ↑ | 64% → 79% ✅ | |
| 수비 라인 ↑ → 상대 오프사이드 ↑ | 62% → **93%** ✅ | |
| 수비 라인 ↑ → 공이 우리 진영에 있는 시간 ↓ | **38% ❌** (9/10) | |
| 팀컬러 +4 | 승률 +10.0 → **+11.8%p** ✅ | 8~15%p |
| 강화 24 → **공격수** 5명 | +7.8%p → **+2.0%p △** (9/10 — 수비 가로채기·블록이 들어가며 같이 깎였다) | 8~15%p |
| 강화 24 → **수비수** 5명 | −0.3%p ❌ → **+2.7%p △** (9/10 수비 강화 뒤) | +4%p 목표 |
| 팀컬러 +1 (5명) | +1.3%p △ | — |
| 홈/원정 대칭 (같은 스쿼드) | 51.5% → **42.5% ⚠** (9/10 verify, 시드 5000+ · N=200) — 그러나 `npm run asym 100`(시드 7000+)은 **51.7%**, 전·후반 점유 50.0/49.8%, 골 홈 1.81 : 원정 1.82. 하프별로 기울지 않아 킥오프·진영 교체 탓은 아니다. **표본 노이즈로 본다.** 다만 원정 슛이 두 하프 모두 +10%(3.65:4.07 · 3.56:4.21)라 지켜볼 것 | 47~53% |
| 능숙도 초록 11 vs 빨강 11 | 82.8% → 87.3% ✅ | ≥70% |
| 같은 시드 100회 해시 | 100/100 ✅ | 100% |
| 홈/원정 대칭 (같은 스쿼드) | 51.5% ✅ | 47~53% |
| 능숙도 초록 11 vs 빨강 11 | 82.8% ✅ | ≥70% |
| 같은 시드 100회 해시 | 100/100 ✅ | 100% |
| 1대1 전환율 (14 m · `npm run oneone`) | 38.8% ✅ | 실측 |

**표본 수를 줄이지 말 것.** 기본값 120 으로 돌리면 홈/원정이 56.7%, 수비 강화가 −7.1%p 로 나온다.
둘 다 표본 노이즈다 — 시드가 `5000+i` 로 고정이라 N 이 바뀌면 표본 자체가 바뀐다. **항상 200 으로 잰다.**

### 카드 데이터 (`npm run clubs 90` · `npm run cards` — 실제 구단)

| 항목 | 1,024장 (예전) | **1,056장 (2026-09-09)** |
|---|---|---|
| 실제 구단 90판 평균 골 | 3.10 | 3.56 △ (기준 2.5~3.5) |
| 1부 vs 2부 승률 | 60.2% | **70.5%** |
| 홈 승률 (같은 부) | 48.8% | 55.8% (n=43, 노이즈) |
| 급여 상한 | 238 | 252 |
| 29개 구단 상한 통과 | ✅ | ✅ (빠듯 FC서울 244 · 여유 파주 64) |

**1부/2부 격차가 벌어진 것은 원본이 그렇기 때문이다** (FC서울 급여 244 vs 파주 64). 버그가 아니다.

**남은 숙제 하나**: 강화 예산을 **수비수에게 쓰면 승률이 안 오른다**(−0.3%p). 공격수는 +7.8%p.
엔진 쪽 문제라 명단 갱신과 무관하게 그대로 남아 있다.

단계 7 후속으로 볼 것 — 태클·마크·위치 선정이 실제로 실점을 줄이는지.

---

## 다음 단계

### 사용자가 먼저

1. **다시 한 판** — 제보 11건이 손에 맞는지: GK 다이브 · `›` 표시 · D 홀드 자동 추격 · 슛 코너 고르기 · 추가시간 자막·휘슬 · 마른 선수.
2. **B-1 다른 회선 두 대로 대전** — 로컬 두 탭은 통과했다. NAT 는 같은 PC 로는 못 잡는다.
3. **공지 채널 정하기** — `docs/공지글-모음.md` 1차 문안이 준비돼 있다. 스크린샷 셋(스쿼드 · 골 · 방향 표시).

### 다음 세션

| 순서 | 무엇 | 끝났다의 기준 |
|---|---|---|
| 1 | 2차 피드백 반영 (위 "다시 한 판") | 사용자가 "됐다" |
| 2 | 파울 빈도 — 합성 120판에서 파울/판을 재고 10~13 으로 | `npm run balance -- 120` |
| 3 | 단계 8 공개 — 공지 올리기 · README 배포 상태 갱신 | 공지 게시 |

---

## 주의사항 & 교훈

앞 세션 것(1~21)은 그대로 유효하다. 이번에 더한 것:

22. **`src/core` 안에서 ball.ts 는 rules.ts 를 import 하지 않는다** (순환). 반칙은 `st.pending` 에 적어 두고 `sim.step` 이 끝에서 `resolvePending` 을 부른다.
23. **세트피스 위치는 매 틱 강제한다.** AI 한테만 맡기면 사람이 조작해서 규칙을 어긴다 (킥오프에 상대 진영 진입 등).
24. **오프사이드는 "표시 → 관여하면 판정"** 두 단계다. 패스가 나가는 순간 위치를 표시(`markOffside`)하고, 표시된 선수가 공을 먼저 만지면 그때 반칙이다. 스로인·골킥·코너는 `clearOffside` 로 지운다.
25. **교체는 선수 객체를 바꾸지 않고 `spec` 만 갈아 끼운다.** 22칸은 고정이고 "누가 그 등번호를 입고 뛰는가"만 바뀐다 — 인덱스가 안 깨져 락스텝·해시가 그대로다. 렌더는 `rebuildRig` 로 그 한 명만 다시 만든다.
26. **효과 크기는 반드시 짝 비교로.** 따로 돌리면 시드 잡음(±6%p)이 효과(+8~15%p)를 덮는다. 팀컬러가 처음엔 −4%p 로 나왔다가 짝 비교·200판에서 +10%p 로 드러났다.
27. **Trystero 의 `makeAction<T>` 는 `DataPayload` 제약이 있다.** 우리 메시지 타입은 그 제약을 못 맞추므로 `makeAction('ctl') as unknown as […]` 로 감싼다.
28. **Claude 브라우저 패널의 배경 탭은 창이 좁다** — 폰 차단(900px)에 걸린다. 두 탭 대전을 시험하려면 `resize_window` 로 넓혀야 한다.
29. 온라인 대전은 **일시정지가 없다.** 내가 멈추면 락스텝 때문에 상대도 멈춘다.
30. **`prompt()`·`navigator.clipboard` 는 실패할 수 있다** (사생활 모드·권한). 스쿼드 코드는 복사 실패하면 화면에 그대로 띄운다.
31. **보호명이 다른 실존 선수의 이름이 될 수 있다.** 성씨만 바꾸면 하필 명단에 있는 이름이 나온다(3건). 원본 이름 전체를 금지 집합으로 놓고 피해야 한다 — `build_cards.py` 가 그렇게 한다.
32. **급여 상한 조건 2 는 포지션을 가리면 안 된다.** 포지션 몫으로 재면 포메이션에 따라 그 몫을 벗어나 상한을 넘는다 (강원 4-3-3 이 231 > 230).
33. **원본 데이터는 저장소 밖에 둔다.** `build_cards.py` 가 경로를 인자로 받고, 실명이 0건인지 확인하고 나서야 파일을 쓴다.

34. **소리 크기는 파형으로 잰다.** 귀로 맞추면 사람마다 다르다. AnalyserNode 를 마스터에 붙여 RMS 최고값을 재면
    "골대가 다른 소리의 3배" 같은 것이 바로 보인다. 계측값은 CHANGELOG 에 남겼다.
35. **AudioContext 는 첫 사용자 입력 전에는 소리를 안 낸다** (브라우저 자동재생 정책). 그 전 이벤트는 그냥 버린다.
36. **테스트 모드는 `?test=1` 로 가린다.** 코드를 지우거나 플래그를 뒤지지 않아도 되고, 배포에서 링크를 안 걸면 그것으로 끝이다.
37. **"잡을 수 있는 거리"와 "손이 닿는 거리"는 다르다** (2026-09-10 GK 제보). 도달 가능 반경 안이면 그 자리에서 잡게 하면 공이 순간이동한다. 몸을 **먼저 옮기고**(다이브 속도) 손 거리에서만 잡아야 한다. 같은 함정이 태클·압박에도 있다 — "닿을 수 있다"를 "닿았다"로 쓰지 말 것.
38. **사람 조작 보조는 "절대 골문 밖을 조준하지 않는다"가 기본값**이어야 한다. 방향키를 어디로 밀든 코너 선택으로만 쓴다. 키가 "빗나가기" 로 해석되면 사용자는 버그로 느낀다.
39. **하프 종료는 시계가 아니라 데드볼로 끝낸다.** 시계만 보면 슛 직전에 끊긴다. 유예(20초)를 두되 무한히 기다리지 않는다.
40. **Read 도구가 "unchanged since last Read" 라고 잘못 볼 때가 있다** (다른 PC 커밋을 pull 한 직후). 그럴 땐 `cat -n` 으로 읽는다 — Edit 는 디스크 내용 기준이라 그대로 된다.
41. 방향 표시처럼 **입력을 화면에 보여 주는 것은 `team.inX/inY` 를 렌더가 읽으면 된다** — sim 에 아무것도 더하지 않는다(결정론 무관).
42. **짧은 CSS 클래스 이름은 반드시 `grep` 하고 짓는다.** `.mini` 를 로비 급여 게이지에 쓴 다음 날 선수 목록 막대에도 `.mini` 를 붙여 게이지가 24px 로 쪼그라들었다(2026-09-11 사용자 제보). 같은 실수를 KMD26 에서도 했다(`slot`). 한 CSS 파일에 화면이 여럿이니 화면 접두어(`sq-` `lobby-`)를 붙이는 편이 안전하다.

---

## 중요 파일 맵

| 경로 | 역할 |
|---|---|
| `docs/DESIGN.md` | **설계서 정본** |
| `docs/DECISIONS.md` | 결정 · **5장 = 사용자 지적 규칙 4건과 단계 7 계측** |
| `docs/PREP.md` | 사용자가 직접 할 것 (A-1 허락 · B-1 다른 회선) |
| `src/core/state.ts` | 상수·타입 (Phase · Restart · PendingCall · SubOrder) |
| `src/core/ball.ts` | 소유·태클·**파울**·패스·**스로인(손)**·슛·**GK 세이브/펀트**·**오프사이드 표시** |
| `src/core/rules.ts` | 킥오프·아웃·**프리킥·PK·카드·교체**·**세트피스 위치 강제** |
| `src/core/ai.ts` | 팀 AI · **압박 슬라이더** · 라인 지키기 |
| `src/core/sim.ts` | createState / step / hashState / snapshot · 사람 입력 해석 |
| `src/cards/cards.ts` · `squad.ts` · `squadcode.ts` | 6스탯·OVR·급여 / 규칙·상한·팀컬러 / 코드 |
| `src/data/cards.json` | **카드 1,056장** — `tools/build_cards.py` 가 굽는다. 손으로 고치지 않는다 |
| `src/data/pool.ts` | `cards.json` 을 풀어 `POOL`·`CLUBS` 로 |
| `tools/build_cards.py` | 원본 명단 → 카드. **보호명·구단명 표(`CLUB_NAME`)가 여기 있다** |
| `src/render3d/kits.ts` | 구단 색 → 유니폼. **홈 단색 · 원정 전신 흰색**(GK 제외) |
| `src/ui/stars.ts` | 별점 HTML · 미니 막대 — 능력치를 숫자로 보이지 않는다 |
| `src/audio/sfx.ts` | **소리 전부** — 관중석·휘슬·킥·골·로비 배경음. 음원 파일 없음 |
| `src/render/keyview.ts` | 키 입력 표시 (테스트 모드) |
| `src/net/room.ts` · `lockstep.ts` | Trystero 로비·방 / 2인 락스텝 |
| `src/game/session.ts` | 경기 루프 (혼자 하기 + 온라인) · 메뉴 · 교체 화면 · 결과 |
| `src/render3d/referee3d.ts` | **심판 3명** (렌더 전용) |
| `src/render3d/camera.ts` | 방송 카메라 수치 — 카메라를 만지려면 여기 |
| `src/ui/squad.ts` · `waitroom.ts` · `lobby.ts` | 스쿼드 화면(저장 슬롯 띠 · 급여 강조 · 강화 상자) / 대기실 / 로비 |
| `docs/플레이-가이드.md` · `docs/공지글-모음.md` | 처음 하는 사람용 · 공지 1차 문안 |
| `tests/feedback.test.ts` | 2026-09-10 제보 — GK 순간이동 · 백패스 · 슛 보조 · 추가시간 · 수비 키 |
| `tools/verify.ts` | **단계 7 검증** — 지문·짝 비교·효과 크기·대칭·결정론 (200판이면 20분 넘게 걸린다) |
| `tools/asym.ts` | 홈/원정 비대칭 진단 — 같은 스쿼드 · 하프별 골/슛/점유 |
| `tools/balance.ts` · `oneone.ts` · `cards.ts` | 봇전 계측 / 1대1 전환율 / 카드·상한 |

### 재현 명령

```bash
npm install
npm test               # 90개
npm run build
npm run dev            # http://localhost:5175/KLO26/
npm run verify 200     # 단계 7 검증 (몇 분 걸린다)
npm run balance -- 120
npm run asym 100       # 홈/원정 비대칭 진단 (같은 스쿼드 · 하프별)
npm run oneone 400 14  # 1대1 전환율
npm run cards          # 카드 OVR 보정 · 급여 상한
npm run clubs 90       # 실제 구단끼리 계측
npm run build:cards    # 원본 명단 → src/data/cards.json (원본은 저장소 밖)
```

### 원본 명단 갱신 (KM26 이 로스터·능력치를 바꿨을 때)

**KM26 은 능력치를 저장하지 않는다.** 갖고 있는 건 표 셋뿐이다 — `D1`(1부 12팀 손으로 매긴 ovr) ·
`D2`(2부 17팀 팀 수준) · `ROSTER26`(29구단 등록 명단 — 이름·포지션·생년월일·등번호·키·몸무게).
능력치 40여 종은 `mkPlayer()` 가 **만들어 낸다.** 그래서 옮겨 적지 않고 원본 함수를 그대로 돌린다.

```
KM26 index.html ──extract_data.py──> gen.js ──go run ./tools/gendata──> data/*.json
                                                                            │
                                          klo26 build_cards.py ─────────────┘──> src/data/cards.json
```

| 저장소 | 주소 | 여기서 하는 일 |
|---|---|---|
| KM26 (원작자) | `github.com/KleagueM2026/KM26v2.0` · 실행 <https://kleaguem2026.github.io/KM26v2.0/> | 명단·능력치 원본 (`index.html` 한 파일) |
| KMD26 | `github.com/goormigrm/KMD26v1.0` | 추출 도구 `tools/extract_data.py` + `tools/gendata`(Go·goja) |
| KLO26 | 여기 | `tools/build_cards.py` — 보호명·구단명·컬럼 형식 |

```bash
cd ../KM26v2.0 && git pull                       # 원본 받기
cd ../KMD26v1.0/KMD26v1.0
python tools/extract_data.py ../../KM26v2.0/index.html src/data/gen.js
cd tools/gendata && go run . -root ../..         # → KMD26 data/*.json
cd ../../../../klo26 && npm run build:cards      # → src/data/cards.json
npm test && npm run cards && npm run clubs 90    # 반드시 다시 잰다
```

**2026-09-09 갱신 때 걸린 것 둘** (다음에도 같은 자리에서 걸린다):

1. `extract_data.py` 의 **패치 GK-01 이 0곳** — 원작자가 `assignPrefPos` 에 같은 고침을 넣었다.
   패치를 빼야 통과한다. 원본이 되돌아가면 다시 살린다.
2. `UID_GEN is not defined` — 새 `nextGenUid` 가 그 상수를 본다. 그런데 선언이
   `const UID_CLUB=1001, UID_COACH=2001, UID_REAL=10000, UID_GEN=900000;` 한 줄에 얹혀 있고,
   `jsclosure.collect_decls` 는 **다중 선언의 첫 이름만 색인한다.** 뿌리에 `UID_CLUB` 을 넣어야 딸려 온다.

**갱신하면 반드시 따라오는 것**

- `POOL_HASH` 가 바뀐다 → **저장된 스쿼드·저장 슬롯·돌아다니던 스쿼드 코드가 전부 버려진다.**
  일부러 그렇게 만든 장치다(id 가 딴 선수를 가리키는 사고를 막는다). 공지에 적을 것.
- 급여 상한(`computeCap`)·OVR 보정(`calibrateOvr`)은 **풀에서 런타임에 다시 뽑으므로 손댈 것이 없다.**
- 카드 장수를 코드·테스트에 못 박지 않는다. 2026-09-09 에 1,024 → 1,056 으로 늘었다.

브라우저 콘솔에서 `__klo.state()` · `__klo.hashes()` · `__klo.net()` · `__klo.snd()`.
개발 서버에서는 `__sfx` 로 소리를 직접 만질 수 있다 (배포 번들에는 없다).

**테스트 모드**: `http://localhost:5175/KLO26/?test=1` — 로비에 보라색 타일이 생긴다 (AI 대 AI 관전 · 키 입력 표시).

---

> 다음 세션 시작 시 다음 입력으로 컨텍스트 복원:
> `@HANDOVER.md 읽고 이어서 작업 진행`
