# CHANGELOG

## 2026-09-16 — ⚖️ 홈/원정 기울기 원인 — `step` 의 팀 번호 순서 셋 (2,000판 47.1% → 50.5%)

사용자 "홈·원정을 바꿔 가며 180판을 돌리니 홈 승률이 41% — 잘못된 현상이니 원인을 찾아 수정해" (HANDOVER 0-e).

- **원인 가르기** — `tools/asymprobe.ts`(새 도구): 같은 스쿼드 대칭 경기를 **진영만 뒤집기(`swapdir`) · 킥오프 팀만 바꾸기(`ko1`) · 둘 다(`both`)** 로 변형해 200판씩. 네 변형 모두 팀 1 이 이겼다(45.5~48.8%) → 좌표·킥오프가 아니라 **팀 번호(0/1)에 붙은 것**. 선수 순회를 거꾸로 돌리자 같은 시드에서 45.6 → 50.3% 로 뒤집혀 `step` 의 순서를 의심.
- **`step` 에 팀 번호에 붙은 순서가 셋 있었다**: ① AI 결정이 이동 루프 **안**에 있어 팀 1(11~21번)은 팀 0 이 이 틱만큼 움직인 뒤에 정했다(한 틱 앞선 정보로 요격·마크) ② 팀 0 이 늘 먼저 정해서 팀 0 소유자가 찬 공은 같은 틱의 팀 1 결정에 보이고, 팀 1 의 패스는 팀 0 에 한 틱 늦게 보였다 ③ 결정 주기 15틱을 `idx` 로 어긋나게 해서 마주 보는 두 선수(팀 0 의 k 번·팀 1 의 k 번)의 반응 시차가 팀마다 달랐다(11 ≢ 0 mod 15).
- **고침** — 결정(22명) → 이동(22명) **두 단계**. 결정은 **공을 가진 팀부터**(없으면 팀 0), 주기는 **자리 번호**(`idx % 11`)로. 순회는 여전히 결정론이고 해시·리싱크는 그대로다(같은 시드 → 같은 결과).
- **2,000판씩 잰 결과** (시드 10000~11999, 4-3-3 같은 스쿼드): 옛 코드 **47.1%**(골 2.00 · 2.14) → ①만 44.0%(1.96 · 2.17 — 오히려 나쁘다) · ①② 47.2%(2.01 · 2.10) · ①③ 45.0%(1.82 · 1.99) → ①②③ **50.5%**(1.91 · 1.91). 셋이 서로 기대고 있어 하나라도 빠지면 기운다(③만 넣으면 같은 자리 둘이 같은 틱에 정하므로 ② 가 더 아프다). 200~300판으로는 ±4%p 노이즈에 묻혀 갈리지 않았다(13차 100판 51.7% 가 그래서 "닫힌" 것처럼 보였다).
- `tests/asym_order.test.ts` 4개 — 결정 순간 아무도 안 움직였다 · 공 가진 팀 먼저 · 자리 번호 주기 · 결정론. 통계는 `npm run asym 500`/`asymprobe` 로 (공개 전 필수).
- `tests/feedback_0911` 체력 테스트 — 시드 하나에서 sta < 0.55 인 필드 선수가 한 명뿐이라 시뮬 순서만 바꿔도 뒤집혔다(10시드 중 4개). 다섯 시드 합산 · 문턱 0.6 으로.
- DESIGN 4.10 "결정 순서" · 4.12 규칙 5 · 4.14 · 12장 표 · DECISIONS G-54 · README 도구 표(`asym`·`asymprobe`).

## 2026-09-16 — 🧹 15차 마무리 · ⚙ 프리셋 기본값 재측정 · 📏 계측 셋

- **15차 [미반영] 네 줄 끝** — D 압박 테스트 켬(원인은 세팅이 아니라 가속 — 반대 방향키를 누른 채 8 m 는 1초에 못 가고 1.7초에 간다) · 브라우저에서 전력질주 모션·골키퍼 다이브 뒤 넘어짐(7틱 → 31틱) 확인 · DESIGN 4.8e(패스 방향 기억·D 압박·전력질주·넘어짐) · 3.1 S/D 행 · 플레이 가이드.
- **공격 프리셋이 뒤집혀 있었다** — 점유 41%·슛 4.8 로 수비 프리셋(57%·8.3)보다 못했다. 템포 '빠르게'가 받자마자 내보내 패스를 죽였고, 빌드업 짧게·수비 방식 1 도 슛을 깎았다. 템포는 세 벌 다 보통, 공격의 빌드업·수비 방식은 2 → **슛 9.1 · 골 2.13**(균형 5.3 · 1.75). DESIGN 4.10b · DECISIONS G-52.
- `npm run tactics -- 16 "이름:line=3,…"` — 임의 프리셋을 같이 재는 인자.
- **계측** — 봇 120판: 골 3.75 · 파울 6.5(27차 통계 버그 수정 뒤) · 경고 0.61 · 퇴장 0.01. 같은 스쿼드 대칭 180판: **홈 41%**(13차 51.7%) — 원정 쪽으로 기울었다. 최근 코드에 진영을 무시한 절대 좌표 조건은 못 찾았다 → 다음 세션 조사.
- 테스트 163개 전부 통과(skip 0). DECISIONS G-52(프리셋) · G-53(15차 마무리).

## 2026-09-15 — 🔍 요구사항 재검증 (`npm run audit`) — 세트피스 5초 · 파울 통계 버그 · 크로스 박스 · 수비 복귀 전력질주

사용자 "이전 요구사항이 제대로 반영되는지 전부 다시 확인해, 축구 게임으로서의 디테일이 부족하다". 봇 vs 봇 12판을 돌리며 **요청했던 동작이 실제로 나오는가를 세는 도구** `tools/audit.ts` 를 만들고, 밑도는 항목을 고쳤다.

| 항목 (저녁 목록 번호) | 처음 | 고친 뒤 | 무엇을 고쳤나 |
|---|---|---|---|
| 11 세트피스 — 코너 박스 안 공격/수비 · 프리킥 벽/공격 | 1.3/3.3 · 0.5/0.0 (배치가 못 선다) | **5.0/8.0 · 3.8/4.3** | 코너·직접 프리킥(36 m 안)은 **5 초**(`BOX_SETPIECE_TICKS`) 기다린다(1.5~1.8 초였다). 배치 이동은 전력질주 |
| 파울 (판당) | 21~27 (한 판 78·96) | **6.3** | **버그**: 하프타임 동안 골키퍼가 든 공에 압박 선수가 붙어 있으면 `contestBall` 이 GK 차징 파울을 틱마다 **통계에만** 더했다(판정은 버려짐). `contestBall` 은 play 에서만, 파울 통계는 rules 가 판정을 적용할 때 센다 |
| 10 사이드 깊숙이 소유 때 박스 안 동료 | 0.16 | 0.55~0.86 (표본 40~60 · 수비 라인 13~15 m · 뛰는 중 4.5명) | 뛰는 대상 32 → 45 m · 자리 4개(컷백) · 마커는 공이 우리 3분의 1 이면 앵커보다 앞에 안 선다 · **수비수가 자리에서 멀면 전력으로 복귀**(라인이 38 → 13 m 내려오는 데 4 초 걸리던 것) |
| 4 서 있는 선수가 공을 본다 | 80% | 83% | 회전 속도 ×0.6 → ×0.9 |
| 헤딩 (판당) | 1.4 | 1.5~2.0 | (세트피스가 서면서) |
| 7 스루패스 성공 | 63% | 63% | 목표 40% — 그대로 |
| 6 골문 쪽으로 오는 상대에 마커 | 56% | 57% | 목표 60% — 백로그 |
| 8/9 GK 골킥 성공 | — | 73% | 오픈플레이 배급은 봇 경기에 거의 없다(캐치가 드물다) — 사람 GK 키·자동 배급은 24차 그대로 |
| 골 (판당) | 4.1 | 3.5 | |

- `tools/foulprobe.ts`(파울마다 가해자·피해자·통계-사건 불일치) · `tools/crossprobe.ts`(사이드 깊숙이 3 초 뒤 박스 인원·오프사이드 라인) 도 남겼다.
- `Ball.passKind`(마지막 패스 종류 — 계측용) 추가.
- 남은 WARN(백로그): 롭 크로스 성공 17~33%(표본 3~10) · 스로인 성공 34~56%(가로채기 31%) · 마커 55~57% · 박스 안 동료 0.5~0.9(뛰는 중인 동료는 4.5명 — 크로스가 그들이 닿기 전에 올라간다) · 시선 82%.
- 수비 복귀 전력질주는 8 m 로 하니 하프타임 체력이 35% 아래로 떨어져(체력 테스트) 12 m·우리 진영일 때로 줄였다.

## 2026-09-15 — 🟥 뒤에서 오는 태클 — 뺏기 어렵고 파울·경고, 명백한 득점 기회면 퇴장 (`fromBehind` 가 뒤집혀 있었다)

사용자 제보 "공격수와 골키퍼 1:1 에서 뒤에서 온 수비수가 태클로 뺏는 장면이 잦다 — 규칙에 안 맞는다".

- **원인은 버그**: `fromBehind(o, q)` 가 처음부터 **뒤집혀** 있었다 — 도전자가 소유자 **앞**에 있을 때 1(등 뒤)을 돌려줬다. 그래서 정면 도전이 "뒤"로, 뒤 도전이 "정면"으로 취급돼 뒤에서 오면 오히려 잘 뺏고 파울은 드물었다. 고쳤다(0 정면 → 1 등 뒤).
- **규칙**: 소유자가 2 m/s 넘게 달리는데 등 뒤(0.6 넘게)에서 도전하면 — 뺏을 확률 ×0.25, 파울 틱당 2~5%, 파울이면 **최소 경고**. 소유자가 골문 30 m 안·공격 3분의 1에서 골문 쪽으로 달리고 앞에 막을 수비수(옆 8 m·선상 6 m 안)가 없으면 **명백한 득점 기회 저지 = 퇴장**(`clearChance`). 슬라이딩도 같다(공을 먼저 건드려도 ×0.4).
- **AI 는 뒤에서 덤비지 않는다** — `contestBall` 이 봇의 뒤 도전을 건너뛰고, 압박 담당(rank 0·1~2)은 뒤에서 쫓는 동안 `press=false` 로 소유자 앞·우리 골문 쪽 4 m 로 달려 **골사이드를 잡은 뒤** 붙는다. 커버(rank 1)와 지역 수비수는 소유자가 앞에서 3~3.5 m 로 오면 맞선다. 뒤에서 덤비는 것은 **사람이 조작하는 선수뿐**이고 그 대가를 치른다.
- 봇 30판: 골 3.97 · 파울 **14.4 → 6.3** · 경고 0.7 · 퇴장 0 · 태클 21.7 (뒤집힌 채로는 퇴장 2.3/판이 나왔다). `tests/tackle_behind.test.ts` 5개.

## 2026-09-15 — 🗑 실사 그래픽 삭제 (찰흙만) — 경기성에 집중

사용자 결정 "실사 그래픽 그냥 삭제해, 버그가 심해 못 봐줄 정도. 찰흙만 남기고 경기성을 살리자" (DECISIONS G-49).

- 뺀 것: `src/render3d/playerReal.ts` · `public/models/player.glb`(2.9 MB) · `tools/char/`(build·inspect) · `docs/캐릭터-교체-절차.md` · 설정 "선수 그래픽" · 로비 preload · `npm run char:build` · devDeps 8개(fbx2gltf · @gltf-transform ×3 · sharp · meshoptimizer · pngjs · jpeg-js).
- 남긴 것: `Rig` 인터페이스(찰흙이 유일한 구현) · `AnimInput.shot/celeStyle` · `.gitignore` 의 `assets/`(로컬 FBX 가 올라가지 않게).
- 예전 저장 설정의 `graphics` 키는 무시된다(로더가 알려진 키만 쓴다).

## 2026-09-15 — 🧍 실사 캐릭터 "완전 난리" 수정 — T포즈 · 루트 모션 · 뼈 이름 접두어

사용자 제보 "실사로 하면 거의 못할 정도, 찰흙은 정상". 원인 셋이 겹쳐 있었다 (`tools/char/inspect.mjs` 와 브라우저 리그 검사로 계측).

- **T포즈로 미끄러지는 선수** — walk 클립이 없어 run 클립으로 두 번째 액션을 만들었는데, three.js `mixer.clipAction(clip)` 은 같은 클립이면 **같은 액션 객체**를 돌려준다. walk 가중치가 run 에 덮여 걷는 속도(2.2 m/s 아래)에서 idle 0 · run 0 → 가중치 합 0 → 바인드 포즈(T포즈). 22명 중 8명이 팔을 수평으로 든 채 걸었다. 이제 로더가 run 을 **복제**해 walk 로 둔다(`CharacterLib.walk` 는 항상 있음, `walkIsRun`).
- **루트 모션** — Mixamo 를 "In Place" 없이 받아 run 이 0.5 초마다 2.5 m, kick(Strike Forward Jog)이 2.8 m 앞으로 나갔다 되돌아왔다. 로더 `stripRootMotion` 이 Hips 위치 트랙의 x·z 를 첫 프레임에 고정(y 는 남김). 빌드 도구도 같은 처리(`--keep-root` 로 끔).
- **뼈 이름** — Ch38 의 뼈는 `mixamorig5Hips` 처럼 숫자가 붙고 GLTFLoader 가 `:` 을 지운다. `mixamorigHips`/`mixamorig:Hips` 정확 일치로 찾다가 전부 놓쳐 킥·세레모니·GK 홀드·등번호 위치 오버레이가 조용히 꺼져 있었다. 정규식(`^mixamorig\d*:?이름$`)으로.
- **킥·패스 클립 타이밍** — 1.3 초 클립(조깅 + 스트라이크)을 0.55 초로 압축해 허둥댔다. 오른 허벅지 회전이 가장 빠른 순간(`strikeTime`: kick 0.38 s · pass 0.42 s)을 자동으로 찾아 그 0.14 초 앞에서 **제 속도**로 0.6 초만 튼다.
- 계측(브라우저 패널, 강제 프레임): T포즈 0/22 · 가중치 합 전부 1 · 달리는 선수 Hips z 고정 · 패스 순간 pass 클립 1.00/run 0.24. 프레임 시간 실사 24 ms · 찰흙 22 ms — 성능 차이는 작다.
- `__klo.renderer()` 디버그 훅 · `tools/char/inspect.mjs`(클립 길이 · Hips 이동 폭 · 차는 순간) 추가.

## 2026-09-15 — ⚙ 팀 전술 7종 · 🎯 개인 전술(역할) · 🎉 세레모니 3종 (리플레이 중엔 안 함)

사용자 12번("영상처럼 포메이션뿐 아니라 팀 전술·개인 전술을 설정하고 경기에 적용") · 3번("리플레이에 이미 세레모니 자세 · 세레모니를 다양하게").
영상은 자막·캡처 모두 못 얻어(자막 없음 · 브라우저 패널 그리기 정지) FC 온라인의 공개된 전술 항목을 기준으로 설계했다.

- **팀 전술 7종** (`core/tactics.ts TEAM_TACTICS`, 프리셋 수비·균형·공격마다 0~4): 예전 넷(수비 라인 · 압박 · 폭 · 멘탈리티)에 **템포 · 빌드업 · 수비 방식** 추가.
  - 템포: 받은 뒤 내보내기까지 34 → 6틱, 침착하면 드리블·홀드 가산.
  - 빌드업: 먼 패스 벌점 ×1.4 → ×0.6, 자기 진영 전진 롱볼 가산, **골키퍼 배급**(짧게 = 4.5 m 만 열려도 26 m 안 발밑 · 길게 = 8.5 m 넘게 열린 사람 없으면 펀트).
  - 수비 방식: 대인마크를 찾는 거리 ×0.6 ~ ×1.4, 지역(0~1)은 우리 골문 30 m 밖 상대는 놔둔다.
  - 멘탈리티가 세트피스 박스 인원(5·6·7)도 정한다.
- **개인 전술** — 선발 자리(1~10)마다 역할 0~3, 자리군마다 뜻이 다르다: ST(침투형·포스트형·폴스 나인) · 윙(안으로·밖으로·침투형) · CAM(침투형·플레이메이커·박스 투 박스) · CM(박스 투 박스·홀딩·플레이메이커) · 풀백(오버랩·스테이·안으로) · CB(전진·스테이·스위퍼). AI 는 계수 7개(`RoleTraits`: 앵커 앞뒤·좌우, 침투 적극성, 박스 우선순위, 지원 러닝 제외, 내려와 받기)만 본다.
- **스쿼드 화면** — 게이지 아래 **⚙ 팀 전술** 접이 패널(프리셋 탭 · 슬라이더 7줄 · 기본값으로), 전술판에서 선수를 고르면 상세에 **개인 전술** 상자. 칩에 역할 이름. 포메이션을 바꾸면 역할은 전부 기본으로(자리군이 달라진다).
- **스쿼드 코드 v2** — 386비트(프리셋 3 × 7 × 3 + 역할 10 × 2). v1 코드는 "규격이 다릅니다". 예전 저장본은 `normalizeSquad` 가 빠진 값(2·0)을 채운다.
- **세레모니 3종** — 두 팔 흔들기 · 한 팔 주먹 + 깡충 · 비행기(두 팔 옆으로, 상체 앞으로). 득점자는 골 수로, 동료는 idx 로 종류가 갈린다(결정론). **리플레이 중에는 세레모니 자세를 안 잡는다**(`animOf` 가 `view.replay` 를 본다) — 리플레이가 골 단계 프레임을 되돌리느라 팔 든 채 슛하던 것.
- 테스트 `tests/tactics.test.ts` 11개(계수 · 앵커 · 홀딩 · 수비 방식 · 빌드업 배급 · 코드 v2 왕복/v1 거부 · 정규화 · 결정론). 전체 24 파일 157개 통과.

## 2026-09-15 — 🧤 골키퍼 조작 · 👀 공 바라보기 · 🧱 세트피스 배치 · 🛡 대인마킹 · 🎯 스루·크로스 · 🧍 Mixamo 캐릭터(Ch38) 적용

사용자 제보 11건(저녁). 12번(팀·개인 전술)은 별도 항목.

- **골키퍼가 공을 가졌을 때** ("가만히 있다 뺏긴다" · "D 를 누르면 상대 골대까지 찬다") — 원인은 **백패스**: 발로 받은 공은 손으로 못 잡아 발에 붙고, 그 순간 조작이 골키퍼로 넘어와 서 있었다. 이제 골키퍼 전용 키 — **D 펀트 · A 손 던지기(롱볼) · S 짧은 패스**, 0.5 초 안에 아무 키도 없으면 AI 가 배급한다. 손에 든 공(holdT)도 같은 키로 바로 내보낸다.
- **GK 배급 안전** ("뜬금없이 상대에게") — 받을 선수 주변만 보던 것에서 **패스 길**(2.5 m 안 상대) 검사 · 20 m 안 · 전방·사이드 가중. 없으면 펀트.
- **공을 본다** — 거의 서 있는 선수는 공 쪽으로 몸을 돌리고, 골키퍼는 움직이면서도 늘 공을 본다(소유자·킥 중·킥커 제외).
- **광고판 기울기** — 그룹(y) → 판(x) 회전 순서로. 골문 뒤 판이 옆으로 삐뚤어졌던 것.
- **스루패스** ("성공률 0") — 목표를 골문 쪽 4~7 m 앞이 아니라 **동료가 달리는 방향 2~4 m 앞**으로, 세기 ×1.15 → ×1.05, 오차 ×1.4 → ×1.15, 후보 거리 가중 절반. 봇 패스 성공 44 → 51%.
- **크로스·박스 침투** — 소유자가 사이드 깊숙이(|y| > 16 · 상대 진영 20 m 안)면 FW·AM·MF 셋이 니어포스트·PK 스팟·파포스트로 뛰어 들어간다. 하이 크로스 낙하점을 받을 선수 **머리 위**로(공 쪽 0.9 m).
- **대인마킹·골문 커버** ("정면으로 달려오는 공격수를 안 막는다") — 우리 골문 쪽으로 달려오거나 우리 진영에 있는 상대를, 다른 아군이 안 잡은 순으로 **골사이드**에서 마크(`Player.markOf`). 커버(rank 1)는 소유자 뒤가 아니라 **골문 쪽 6~8 m**. 찾는 거리는 봇 난이도(press)에 비례.
- **세트피스 배치** — 코너·직접 프리킥(골문 36 m 안): 공격은 킥커 뺀 여섯 명(FW·AM·MF 먼저)이 니어·파포스트·PK 스팟·6야드·엣지로, 나머지는 하프라인. 수비는 프리킥이면 **벽 3~4명**(9.15 m, 어깨 간격 0.65), 나머지는 박스 안 상대 **골사이드 대인**, 남으면 골문 앞 지대. 확인: 코너 박스 안 공격 5·수비 9, 프리킥 벽 4.
- **Mixamo 캐릭터 Ch38 적용** — 사용자가 받은 FBX(캐릭터 + Breathing Idle · Fast Run · Soccer Pass · Strike Forward Jog)를 `npm run char:build` 로 → 2.96 MB · 삼각형 26.8k · 텍스처 1024. 셔츠·반바지·양말이 **따로 메시**라 부위별로 정확히 팀색. **킥·패스 클립**이 있으면 절차적 킥 대신 클립(슛 = kick, 패스 = pass). 직진 걷기 클립이 없으면 run 을 느리게. 변환 도구: 노멀맵 제거 · 텍스처 1024(pngjs/jpeg-js 순수 JS — sharp 는 16비트 PNG 에서 죽었다) · 메시 단순화(meshoptimizer, 48k → 27k).
- 봇 30판: 골 3.6 · 패스 51% · 파울 15 · 태클 28 (마킹 강화로 접촉이 늘었다 — 120판 재확인은 백로그).

## 2026-09-15 — 🎬 P4 연출 — 리플레이 2 앵글 · 골망 출렁임 · 하프타임 통계 · 세레모니 클로즈업

개발계획 4장 마지막 단계. 전부 렌더·세션 전용.

- **리플레이 2 앵글** — 앞 절반은 **골문 뒤**(골문 뒤 13 m · 높이 6.5 m, 공을 본다), 뒤 절반은 **측면 낮은 컷**(공 뒤 9 m · 높이 3.2 m · 카메라 쪽 사이드). 방송처럼 컷은 즉시. `ViewInfo.replayCam/replayGoalX`.
- **골망 출렁임** — 골 이벤트에 그 골문 뒤·위 그물이 공이 들어간 자리 중심으로 감쇠 진동(0.55 m · 1.4 초). `Pitch3D.netHit/update`.
- **하프타임 통계** — 전반 종료 5 초 동안 결과 화면과 같은 통계 표(`statsTable` 공유). 경기는 멈추지 않고 후반 킥오프가 되면 닫힌다.
- **세레모니 클로즈업** — 득점자를 따라가는 카메라 FOV 24 → 19.
- 확인: 골망·골문 뒤 앵글·하프타임 표는 브라우저에서 봤다. **측면 컷은 브라우저 패널이 그리기를 멈춰(rAF 0회) 눈으로 못 봤다** — 다음 세션 첫 확인.

## 2026-09-15 — 🎸 락 배경음악 (로비·대기실 / 경기 별도) · 📣 관중 응원 리듬

사용자 요청 "신나는 락으로, 대기실과 경기 중 따로, 경기 중 관중 함성". **전부 코드로 만든다** — 음원 파일 없음(DESIGN 7.3 원칙 그대로).

- **로비·대기실** `lobby` 트랙 — 126 BPM 앤섬: 오픈 파워코드 E–C–G–D(2마디씩), 킥·스네어·하이햇, 8마디마다 크래시, 짝수 마디 리드 아르페지오.
- **경기** `match` 트랙 — 150 BPM 드라이빙: 팜뮤트 8분 파워코드 E–E–G–A–E–E–C–A, 더블 킥, 4마디마다 펜타토닉 리프, 8마디째 톰 필인. 관중보다 작게(레벨 ×0.55). 예전엔 경기 중 음악을 껐다.
- 악기는 오실레이터·노이즈뿐 — 파워코드 = 톱니파 근음·5도·옥타브 → 웨이브셰이퍼 디스토션 → 로우패스(팜뮤트 900 Hz / 오픈 2600 Hz), 베이스 = 사각파+로우패스, 드럼 = 사인 스윕 킥·노이즈 스네어(+톤)·하이햇·크래시. 16분음표 스케줄러가 0.25 초 앞을 놓는다(`musicSchedule`).
- **관중**: 슛 순간 "우—"(swell 0.35), 킥오프 뒤 3 초·**골 뒤 6 초**·종료 뒤 5 초 동안 **북·박수 응원 리듬**("짝짝 짝짝짝", `chant`). 골·선방·골대 함성은 그대로.
- 설정 소리 끄기/켜기는 경기 중 트랙(`match`)도 다시 튼다. 로비로 나가면 로비가 앤섬으로 바꾼다.

## 2026-09-15 — 🫨 헤딩 — 공중볼을 머리로 슛 · 패스 · 클리어

사용자 제보 "헤딩 골이 되나?" — **안 됐다.** 공중볼은 머리로 트래핑(발에 붙이기)만 했고 2.2 m 위 공은 못 닿았으며 판정 반경도 발 기준이라 크로스가 머리에 걸리는 일이 드물었다.

- 새 액션 `ACT_HEAD`(10틱 점프). 공중볼(1.0 ~ 2.45 m)이 **머리 반경**(0.8 + 0.35·헤더, 받을 크로스면 +0.25) 안에 오면:
  **사람** — 누르고 있는 키로 **D 헤딩 슛 · S 헤딩 패스 · A 헤딩 클리어**, 아무것도 없으면 트래핑(예전 그대로).
  **AI** — 상대 골문 24 m 안에서 아군 크로스면 슛, 자기 진영 깊으면 클리어, 상대 공이면 클리어 쪽, 아니면 절반은 패스.
- 헤딩 슛: 골키퍼가 비운 코너로 내리꽂는다(속도 9 + 9·헤더, 오차 5°·(1.4 − 헤더)). 닿을 확률 0.5 + 0.45·헤더, 헛헤딩은 공을 스친다. 골키퍼 판정·유효슛 집계는 발 슛과 같다(`markOnTarget` 헬퍼로 공유).
- 렌더: 점프하며 상체를 젖혔다 앞으로(찰흙·실사). 키 안내에 "공중볼 D 헤딩 슛 · S 헤딩 패스".
- 코너킥 A(하이 크로스)·Q+A 하이 크로스 뒤 받을 선수가 공 쪽으로 달리는 기존 로직과 넓어진 머리 판정이 만나 헤딩 골이 난다.
- 테스트 145 → **147개** (`tests/header.test.ts` — 사람 D 헤딩 슛 · AI 박스 헤딩).

## 2026-09-15 — 🏃 P3 엔진 움직임 — 침투 러닝 · GK 다이브 2종 · 태클 넘어짐

개발계획 2장 "움직임의 질" 셋 중 셋(드리블 터치 간격은 뺏기 판정 경로가 바뀌어 별도 단계). 전부 `src/core` 안 · 결정론 유지.

- **침투 러닝** — 공격수·공격형 MF 가 소유자보다 앞에 있고(2~26 m), 소유자가 전진 중이거나 상대 진영이며, 내 앞 7 m 가 비었으면 **오프사이드 라인 바로 뒤까지 사선으로 전력 질주**한다(`Player.runT`). 지원 러닝(rank < n)보다 먼저 본다. 소유자 AI 는 침투 중인 동료에게 점수 +0.22 · **스루 패스**를 고른다.
- **GK 다이브 2종** — 몸을 날릴 때 경로 위 공 높이를 예측해 1.1 m 위면 **뛰어오르는 하이 다이브**(속도 ×0.9, 발밑 공은 놓친다), 아니면 **낮게 눕는 로우 다이브**(1.3 m 위 공은 놓친다). `Player.diveHigh`. 렌더는 하이 다이브에서 몸이 떠오르고 실사는 두 팔을 위로 뻗는다.
- **태클 넘어짐** — 스탠딩 태클로 뺏기면 힘 비율에 따라 20~75% 확률로 14틱 넘어지고, 슬라이딩에 걸리면 항상 18틱 넘어진다. 넘어진 동안 못 움직인다(태클의 보상).
- 봇 30판: 골 3.7 · 파울 8.2 · 태클 21.3 · 패스 44% (넘어짐 완화 뒤 — 항상 넘어지게 했을 땐 파울 19.7)
- 테스트 142 → **145개** (`tests/p3_movement.test.ts`).

## 2026-09-15 — 🎥 P0 화면 인상 (카메라 · 잔디 · 조명 · 광고판 · 관중석 · 표시 정리) + 실사 모션 수정

개발계획 3장 P0 — 전부 렌더 전용. 시뮬·락스텝은 그대로.

- **카메라** 38/30 m → **32/24 m**, FOV 30/25 — 선수가 1.3 배 크게. 골문에 가까워지면 **낮은 측면 컷**(거리 −10 · 높이 −12)으로 천천히 내려간다. `camera.ts` `CamTarget.near`.
- **그림자**를 공 주변 52 × 40 m 에만(태양·과녁이 공을 따라감) — 선명해지고 실사 22명 그림자 패스가 가벼워졌다. 반구광 1.0 · 태양 2.4 · 노출 1.08.
- **잔디** 두 방향 격자(가로 줄무늬 + 세로 깎기 자국) · 노이즈 22k · `MeshStandardMaterial` 광택.
- **광고판 띠**(피치 둘레, "개리그 온라인 2026 · KLO26" + 홈 색 띠 — 실제 브랜드 없음) · **관중석 2단 + 지붕**(카메라 쪽 사이드는 1단만 — 2단·지붕이 카메라 아래 시야를 가렸다).
- **표시 정리** (FC 온라인 밀도): 조작 선수 = **팀색 작은 삼각형 + 이름**(예전 노란 콘 회전·번호 이름표), 공 가진 상대 = 이름만. 발밑 링·방향 `›`·상대 링은 ⚙ 설정 **"도움 표시"**(기본 켬)로.
- **파워 바를 조작 선수 발밑**으로(노랑 → 주황 → 빨강), HUD 의 바는 뺐다. **스코어보드 좌상단 작게**, **레이더 반투명**(하단 가운데는 스로인 킥커를 가려 오른쪽 아래 유지), 같은 배너가 2.5 초 넘게 떠 있으면 **작게 접힌다**.
- **실사 모션 수정** (사용자 제보): 모델이 −z 를 봐서 **반대로 보고 뛰던 것** → 180° 회전. 스로인·킥·GK 홀드·**골 세레모니**(새로) 동작을 뼈 로컬 축 가정 대신 **월드 방향으로 뼈를 겨누는** 방식으로 — 어느 Mixamo 리그든 같은 결과. 찰흙도 세레모니 팔 들기.

## 2026-09-15 — 🧍 실사 선수 그래픽 (P1-a PoC) — glTF 스킨드 메시 + 모션캡처 클립

개발계획 5.4 옵션 B 의 첫 단계. **파이프라인은 완성, 캐릭터 파일은 임시**.

- `src/render3d/playerReal.ts` — `public/models/player.glb`(Mixamo 뼈대, idle/walk/run) 를 `GLTFLoader` 로 받아 22명을 `SkeletonUtils.clone` + `AnimationMixer` 로.
  걷기↔달리기 가중치 블렌드, 재생 속도를 sim 속도에 맞춰 발 미끄러짐을 줄인다. 킥·슬라이딩·다이브·넘어짐·스로인·GK 홀드·전력질주 기울기는 **뼈 회전 덧씌우기**(절차적) — 축구 클립이 오면 그 부분만 교체.
- **유니폼**: 텍스처를 캔버스에서 피부색 픽셀은 남기고 나머지를 옷 평균 밝기로 정규화한 뒤 구단 색으로 물들인다. 등번호는 척추 뒤 작은 판.
- 찰흙·실사가 같은 `Rig` 인터페이스(root · height · animate · dispose) — 렌더러는 구분하지 않는다. ⚙ 설정 **"선수 그래픽: 실사 / 찰흙"**(기본 실사, 경기 도중 전환). 로비에서 파일을 미리 받고, 못 받으면 찰흙으로 남는다.
- 임시 캐릭터는 three.js 예제의 **Soldier**(Mixamo 캐릭터, 헬멧 군인, 11k 삼각형, 2.1 MB) — 실사 비율·모션캡처 달리기·팀색 물들이기·성능을 검증했다. **최종 인간 캐릭터는 Mixamo 에서 받아야 한다** → [docs/캐릭터-교체-절차.md](docs/캐릭터-교체-절차.md).
- 성능: 22명 실사 50만 삼각형(그림자 패스 포함) · 드로우콜 125~180 · **30~51 fps**(찰흙 60). 그림자맵 4096 → 2048. 남은 최적화는 P0(그림자 범위를 공 주변으로) 에서.
- **P1-b 변환 도구** `npm run char:build -- <FBX 폴더>` (`tools/char/build.mjs`) — fbx2gltf 로 FBX→GLB, @gltf-transform 으로 애니 클립을 뼈 이름으로 캐릭터에 이식(파일 이름 = 클립 이름), dedup·prune·resample·quantize(위치·법선·UV 만). Blender 불필요.
  Mixamo 샘플(`Samba Dancing.fbx`)로 검증 — 2.6 MB · 스킨 2 · 클립 53채널. devDependencies: fbx2gltf 0.9.7 · @gltf-transform/{core,extensions,functions} 4.5.
- 다음: **사용자가 Mixamo 에서 인간 캐릭터 + 축구 클립 FBX 를 받아** `C:\assets\mixamo\` 에 → `npm run char:build` → 축구 클립을 `playerReal.ts` 에 연결(P1-c) → P0 화면 인상.

## 2026-09-15 — 📋 개발 계획서: FC 온라인 대회 영상 벤치마크 · 실사 캐릭터 검토

- 사용자가 준 **2025 FSL SUMMER wonder08 vs Exito** 영상을 열 시점으로 정지 화면 분석 → [docs/개발계획-FC온라인-벤치마크.md](docs/개발계획-FC온라인-벤치마크.md).
- **매치엔진 차이**: 오프 더 볼 침투 · 패스 속도·직진성 · 드리블 터치 간격 · GK 다이브 2종 · 태클 넘어짐 · 속도감. 핵심은 "움직임의 질" 셋(침투 러닝·터치·다이브) — 전부 `src/core` 안에서 결정론 유지.
- **화면 차이**: 카메라 거리(38 → 32 m)·골문 근접 낮은 컷 · 잔디 격자+노멀 · 그림자 선명 · 광고판·2층 관중석 · **표시 밀도 줄이기**(삼각형 커서 + 성) · 스코어보드 좌상단 · 파워 바 발밑 · 레이더 하단 · 배너 축소. 전부 렌더 전용·난이도 하 → **P0(3~4일)**.
- **캐릭터**: 옵션 A(프리미티브 비율·재질 개선) · **B(Mixamo 실사 캐릭터 + 모션캡처 → GLB → SkinnedMesh, 권장)** · C(CC0 로우폴리) · D(Ready Player Me, 미확인). 라이선스 확인 — Mixamo 는 무료·상업 가능(FAQ 직접 확인), Quaternius QAL(게임 포함 배포 OK), Kenney·Poly Haven·ambientCG CC0. 원본 FBX 는 저장소에 넣지 않고 변환 GLB 만. 실존 인물 얼굴·피부색은 계속 하지 않는다.
- 로드맵 P0 화면 인상 → P1 캐릭터 PoC(1명 + 애니 6개, 22명 60 fps, ≤ 8 MB, 찰흙/실사 토글) → P2 전면 → P3 엔진 움직임 → P4 연출.

## 2026-09-11 — 🎮 패스 방향 · D 압박 · 전력질주 모션 · 골키퍼 다이브 뒤 일어나기 (15차 · **중간 커밋**)

사용자 제보 4건(저녁). 코드는 들어갔고 tsc·테스트 통과(1개 skip). **브라우저 검증·문서(DESIGN·가이드) 갱신은 다음 세션.**

- **패스 방향** — S/D 를 떼는 순간 방향키가 (0,0)이면 바라보는 방향으로 갔다. **방향키를 놓은 뒤 0.25초 안**은 마지막 방향(`Team.aimX/aimY/aimT`).
  후보 점수는 각도 무게 절반 + 거리 −0.035/m — **방향키 쪽 가까운 선수**가 먼저다(정면 20 m 보다 23° 옆 7 m).
- **D 압박** — 목표를 공이 아니라 **소유자의 0.35초 뒤 자리**로, 방향키 비중 45% → 25%(반대로 누르면 거의 안 갔다), 압박 중 **자동 전력질주**.
- **전력질주 모션** — 보폭 주기 ×1.25 · 팔 스윙 ×1.6 · 팔을 굽혀 앞으로 · 상체 앞으로 · 위아래 흔들림 ×1.5 (`AnimInput.sprint`, 사람은 E 홀드 · AI 는 `p.sprint`).
- **골키퍼 다이브 뒤 일어나기** — 다이브 24틱 뒤 `ACT_FALLEN` 30틱(0.5초). 그동안 잡지 못하고 누운 몸(0.6 m)에 맞은 공만 튕긴다. 잡았으면 바로 선다.
  렌더는 옆으로 누운 자세 그대로 천천히 일어난다(`rig.wasDive`). 예전엔 곧바로 반대쪽으로 또 날아 순간이동처럼 보였다.
- 봇끼리 30판: 골 4.0 · 파울 15 · 태클 25 (골키퍼가 0.5초 못 잡는 만큼 리바운드 골이 조금 늘었다 — 잡음 범위, 120판 확인은 백로그).
- 테스트 136 → **141개**(`tests/feedback_0911b.test.ts` 5개, 그중 D 압박 1개 skip — 세팅 미완).

## 2026-09-11 — 🧤 골키퍼 선방 현실화 · 🔁 교체 5명/3회 전술판 · 🎯 프리킥 조준·궤적 · 체력 · 스로인 · 파울

사용자 제보·요청 8건(오후). 전부 **먼저 재고**(`shots` · `stamina` · `throwin` 도구) 고쳤다.

- **골키퍼가 다 막았다** ("유효슛 16개에 골 0, 드리블로만 넣었다"). 계측 — 유효슛 중 골 **9%**, 쳐냄 67%. 셋을 고쳤다:
  사람 슛 자동 조준이 겨누던 "비운 코너"가 중앙에서 **1.6 → 2.7 m**(골키퍼가 서서 손만 뻗어도 닿던 자리였다),
  쳐내기 확률 **78% 고정 → 몸 정면 0.75 · 손끝 0.2**(뻗은 정도·속도·결정력), 다이브 속도를 도달(`gkReach`) 기준으로.
  결과 유효슛 중 골 **37%** — 12 m 정면 37~56% · 16 m 30% · 20 m 13~26% · 25 m 20%. 봇끼리 골 3.1 → 3.4/판.
- **골키퍼가 드리블러의 공을 덮친다** — 예전엔 절대 뺏지 않아 옆으로 걸어 들어갈 수 있었다. 3 m 안이면 손으로(틱당 3%·핸들링), 잡으면 손에 든다. 덮치다 사람을 잡으면 드물게 PK.
- **교체 — 기회 3번에 최대 5명.** Esc → 교체가 **전술판**이 됐다 — 세로 전술판의 뛰는 선수(체력·카드 표시)와 후보를 하나씩 눌러 짝을 만들고 확정.
  **다음 경기 중단**(스로인·골킥·코너·프리킥·골)에 한꺼번에 들어가고 그때 기회를 하나 쓴다. 하프타임 교체는 기회를 안 쓴다. 다시 열면 넣어 둔 짝을 고칠 수 있다.
  명령은 Input(`BTN_SUB`, 한 틱에 한 명 · `SUB_CLEAR`)으로만 가서 온라인도 같은 길. HUD "교체 5명 · 기회 3번 · 대기 2명", 들어갈 때 자막.
- **직접 프리킥·PK 조준** — 키커 뒤 시점에서 방향키가 **화면 기준**이 된다: **← → 코너 · ↑ ↓ 높이**, D 홀드 힘.
  누르고 있는 동안 **예상 궤적**이 구슬 점선으로 보인다 — 골문 안이면 금색, 밖이면 흰색 + 낙하점 링, A 홀드 롱볼·골킥 펀트는 하늘색.
  미리보기와 실제 킥은 같은 계산(`aimShot` · 난수 없음)이라 오차만 다르다. 시점 조건은 sim 의 `kickerView` 한 곳 — 렌더 카메라·HUD·입력 해석이 다 그것을 본다.
- **프리킥을 기다리는 동안 공격수는 오프사이드 위치에 서지 않는다** — 뒤에서 두 번째 상대·공·하프라인 중 가장 앞선 곳까지만 (매 틱).
- **체력** — 하프타임 평균 15% · 종료 1% 였다(제보 "전반 끝인데 다 0"). **경기 길이에 맞춰** 닳게(`90 / halfSec` 배율) + 카드 체력(sta) 폭을 넓혔다(0.3 → ×1.18 · 0.9 → ×0.64).
  하프타임 51%, 종료 때 체력 낮은 선수 5% · 높은 선수 27%. 하프 2·3·4분 모두 같은 분포.
- **스로인** — 공이 2 m 높이로 아군 머리 위를 넘어 상대 발 앞에 떨어졌다(`vz = ½GT` 는 출발 높이로 돌아오는 포물선). 41% 를 상대가 가로챘다.
  받을 선수 발 근처(0.3 m)에 떨어지게 → 24%. ← → 로 라인과 평행하게 던져도 안쪽 30° 로 틀어 **바로 아웃(상대 스로인)이 되지 않는다**.
- **D 압박 파울** ("거의 다 파울"). 붙어 있는 1초 동안 파울이 거의 확실했다(틱당 1~6.5%). 정면 1/3 · 뒤 2/7 · 성향 1/4 로 — 판당 파울 17.7 → **13**, 태클 성공 16 → **26**.
- **속도 폭 한 번 더 축소** — 체력을 덜 지치게 하자 속도 A/B 가 72.3 → **79.8%** 로 다시 슈팅(75.3) 위로 올라왔다(13차의 72.3 은 전원 탈진 덕이었다).
  스프린트 소모를 올리는 것(0.04 → 0.065)은 속도를 못 내리고(78.8) 슈팅만 떨어뜨려 되돌렸다. 최고 속도 폭 +14% → **+11%**(7.6→8.5), 가속 +25% → **+19%**(6.7→8.0)
  → 속도 **74.8%** · 슈팅 73.5% (100판). 체력 분포는 그대로(하프타임 48% · 종료 낮은 7% / 높은 25%).
- 도구 셋 — `npm run shots`(자리별 슛 전환율) · `npm run stamina`(하프타임·종료 체력) · `npm run throwin`(스로인 결과).
- 테스트 121 → **136개** (`tests/subs.test.ts` 7 · `tests/feedback_0911.test.ts` 8).

## 2026-09-11 — 🎯 세트피스: 키커 뒤 시점 · A 홀드 롱볼

- **직접 프리킥·페널티킥은 키커 뒤에서** 골문을 봅니다 (가운데 쪽으로 살짝 비켜 비스듬히). 찬 뒤 1.4 초 공을 따라가다 방송 카메라로 돌아옵니다. 배너에 골문까지 거리.
- **프리킥·골킥 롱볼** — A 를 홀드했다 떼면 방향키 쪽 먼 지점으로 로빙(18~50 m). 예전엔 가까운 동료에게 짧게만 갔습니다(제보). 골킥 D 홀드 펀트·프리킥 D 홀드 슛은 그대로.
- **PK 골키퍼는 킥 순간까지 골라인 가운데** — 골키퍼 AI 가 각을 좁히려 5 m 앞으로 나오던 것을 막았다.
- 테스트 117 → **121개** (`tests/setpiece.test.ts`).

## 2026-09-11 — 🎯 결정력이 골키퍼를 이기고, 속도는 값을 치른다

- 사용자 지적 그대로: **잘 찬 공은 골키퍼가 못 잡는다.** 찬 선수의 결정력을 공에 남겨(`shotQ`) 잡기 최대 −19%p·쳐내기 흘림, 결정력이 높으면 슛도 빠르다.
- **전력 질주 중 슛은 자세가 불안하다** — 오차 최대 +120%·힘 −15%·공이 뜬다 (결정력이 높으면 덜 흔들린다).
- **전력 드리블은 뺏긴다** — 태클 확률 최대 3배. **전력으로 달리며 받는 퍼스트 터치**는 −18%p.
- **짧은 경합은 예측이 가른다** — 요격 반응 시간 0.35 → 0.10 s 를 위치 선정(posn)에 걸었다.
- 200경기 A/B: 수비 83.0 · 드리블 78.8 · 슈팅 **72.8** · 속도 **72.3**(처음 89.8) · 골키퍼 64.3.
- 테스트 115 → **117개** (`tests/finishing.test.ts` — 결정력 92 vs 38 코너 슛 200번, 달리며 찬 슛).

## 2026-09-11 — 🏃 속도 지배 완화 · ⚽ 골 세레모니 · 🔁 리플레이

- **속도가 경기를 지배하지 않게** — 속도 A/B 승률 89.8% → **77.0%**(수비 74.8 · 드리블 71.5 · 슈팅 66.5).
  최고 속도 폭 +24%→+14%, 가속 +51%→+25%(실제 100 m·10 m 기록 수준), **공 몰고 뛰는 속도 상한**(6.6~7.5 m/s),
  체력 절반 아래부터 연속 감속, 전력 드리블은 공이 발에서 멀어져 뺏기기 쉽다. DESIGN 4.3a.
- **골 세레모니** — 골 뒤 9 초. 넣은 팀이 득점자에게 모이고 득점자는 코너로, 먹은 팀은 걸어 돌아간다.
  **Enter** 로 건너뛰기. 온라인은 **양쪽이 다** 눌러야 넘어간다(HUD 가 상대 상태를 알려 준다).
- **골 리플레이** — 골 직전 4.5 초를 0.9 배속·바짝 당긴 카메라로. 렌더 전용이라 온라인 락스텝과 무관.
  Enter 로 내 화면에서 즉시 끝낼 수 있다.
- 테스트 111 → **115개** (`tests/celebration.test.ts`).

## 2026-09-11 — 🔬 능력치·전술 검증 · 패스 받기 · 공 가진 선수 표시 · 입력 키 · 공지 사진

- **능력치가 경기에 닿는지 전부 쟀다** (`npm run attrcheck`, 묶음별 200경기). 슈팅은 닿았고(74%),
  **태클·마크·패스·골키퍼는 49~51% 로 사실상 안 닿았다.** 넷 다 손봐 56~68% 로 올렸다 — 표는 DESIGN 4.5a.
  안 읽히던 활동량(wor)·시야(vis)·GK 공중장악(aer)·펀칭(pun)도 이었다.
- **전술은 실제로 움직임을 바꾼다** (`npm run tactics`) — 라인 높이 수비 44 m · 균형 50 m · 공격 53 m.
  "전술 균형" 은 잔재가 아니라 지금 프리셋이라 `전술 균형 · [ ] 로 바꿈` 으로 뜻이 보이게 했다.
- **패스 받을 선수가 딴 데로 안 뛴다** — 패스가 살아 있는 동안 조작이 받을 선수로 넘어가고 잡을 때까지
  공 쪽으로 스스로 달린다(방향키 35%). S 로 바꾸면 존중. 상대 가로채기는 그대로. `tests/receiver.test.ts`.
- **공을 가진 선수에게 링과 이름** — 조작 선수가 아니어도. 아군 흰색 · 상대 붉은색.
- **입력 키 표시** — 왼쪽 아래에 지금 누르는 키(방향키 빼고)와 조합(`Q + D`). ⚙ 설정에서 켜고 끈다(기본 켜짐).
- **공지글 사진·GIF** — 개발 서버 `/__snap` + `src/debug/shot.ts` 로 `docs/img/` 에 PNG·GIF 를 뜬다
  (bedorage-duck 방식, 저장소엔 안 넣는다). 콘솔 `__shot('이름.png')` · `__klo.snap/gif/gifDom`.
- 테스트 108 → **111개** (`tests/receiver.test.ts`).

## 2026-09-11 — 🤝 팀컬러 → 팀워크 · 타 구단 영입 제한 · 구단 이름 · 교체 후 선택 해제

사용자 요청 여섯 가지를 한 묶음으로.

- **교체·영입 뒤에는 선택이 풀린다.** 자리 클릭·끌어놓기·⬇후보로·빠른 맞바꾸기·목록에서 넣기 전부.
  다음 클릭이 실수로 또 교체되지 않는다.
- **"급여가 예산에 비해 싼 것 아니냐" — 맞았다.** 제한이 없을 때 29개 구단 **전부** 최강 선발 OVR 92~94 로
  수렴했다(격차 8.1). 어느 구단을 골라도 같은 올스타 팀이 된다는 뜻이다.
  상한(252)은 "어느 구단이든 자기 선수 18명은 들어간다"는 조건 때문에 못 내리는데 그 값이 곧 올스타 값이라
  **상한 하나로는 못 푼다.**
- **타 구단 영입 제한** — 선발 5명 · 후보 2명 (`OUT_XI_MAX` · `OUT_BENCH_MAX`).
- **영입 웃돈 ×1.5** — 다른 구단 선수는 급여가 1.5배. 자기 구단 선수에는 안 붙어 상한 조건이 유지된다.
  상한 사용 평균 186 → **224/252** 로 올라 예산이 다시 선택을 만든다. 센 구단은 2명, 약한 구단은 5명까지 데려온다.
- **팀컬러 → 팀워크.** 뜻을 알기 어려운 이름이었다. 화면에도 **뭉침 / 약체 가산 / 무엇이 오르는지**를 나눠 적었다.
- **약체 가산** — 전력(그 구단 최고 18명 급여 합)이 낮을수록 팀워크가 더 붙는다(+0~3).
  뭉쳤을 때만 얹는다. 최저 구단 최강 선발 62.8 → **77.6**, 구단 격차 16.5 → **11.4**.
- **구단 이름을 전부 바꿨다** — 개리그 매니저(KM26)의 구단표를 그대로 옮겼다(29개). 선수 보호명과 같은 뜻이다.
  `POOL_HASH` 는 선수 id·능력치에서만 나오므로 **저장해 둔 스쿼드는 그대로 산다.**
- **구단 고르기를 1부·2부로 나눠** 세우고, 카드마다 약체 가산 태그를 붙였다.
- 규칙이 바뀌어 급여·영입 인원이 어긋난 저장 스쿼드는 **버리지 않는다** — `SquadCheck.hard` 를 나눠
  못 고치는 문제(선수 수·골키퍼·중복·지문)만 버린다.
- 도구 둘 추가 — `npm run economy`(구단별 최강 선발·격차) · `npm run power`(구단 전력).
- 테스트 97 → **108개** (`tests/signings.test.ts` 10개).

## 2026-09-11 — 🐛 원정이 걸리면 내 선수가 안 움직이던 버그

- 사용자 제보: "혼자 하기에서 내가 원정이 걸리니까 선수가 움직이지 않았어".
- 원인 — 동전 던지기(2026-09-11)로 사람이 **원정(팀 1)** 을 맡을 수 있게 됐는데,
  세션이 내 입력을 늘 **0번 칸**(홈)에 넣고 있었다. 봇이 내 키를 받고 내 팀은 빈 입력을 받았다.
  sim 은 사람 팀의 조작 선수를 AI 로 움직이지 않으므로 **한 발짝도 움직이지 않았다**(실측 0.00 m).
  교체 명령도 같은 입력에 실려 가므로 함께 먹히지 않았다.
- 고침 — `core/input.ts soloInputs(meTeam, inp)` 로 **내 팀 칸에** 넣는다.
  실측 원정 0.00 m → **4.38 m**(60틱 방향키). 브라우저에서도 원정 3.46 m 확인.
- `tests/away_control.test.ts` 3개 추가 — 홈·원정 둘 다 움직이는지, 0번 칸 고정이면 얼어붙는지.

## 2026-09-11 — ⚙ 경기 중 설정을 로비와 같은 화면으로 · 로비로 버튼 분리

- 경기 오른쪽 위를 **로비로 · ⚙ 설정 · 메뉴(Esc)** 셋으로 나눴다. 예전엔 Esc 메뉴 안에 다 섞여 있었다.
- **설정 패널을 로비와 함께 쓴다**(`ui/settings.ts`) — 소리 · 그림자 · 렌더 해상도 · 조작 안내 띠를 켜기/끄기 세그먼트와 한 줄 설명으로. 누르면 **즉시** 화면에 반영되고 브라우저에 저장된다. 전부 내 화면 설정이라 상대에게 보내지 않는다.
- 그림자·해상도를 **경기 도중에** 바꿀 수 있게 렌더러에 `setShadows`/`setResScale` 을 넣었다. 그림자는 `shadowMap.enabled` 를 바꾼 뒤 모든 재질을 `needsUpdate` 해야 셰이더가 다시 컴파일된다.
- Esc 메뉴에는 **행동만** 남겼다 — 계속 · 교체 · ⚙ 설정 · 로비로. **로비로는 한 번 묻는다**(경기가 끝났으면 바로 나간다).
- 버그 — Esc 가 **온라인에서 메뉴를 닫지 못했다**. `toggleMenu` 가 `paused` 로 판단했는데 온라인은 락스텝이라 언제나 `paused === false`. 창이 떠 있는지(`overlay.hidden`)로 바꿨다.

## 2026-09-11 — 🪙 홈·원정을 동전 던지기로 · 경기장이 홈 팀 색으로

- **방장이 늘 홈이던 것을 없앴다.** 홈/원정은 경기마다 **동전 던지기** — 결과를 **경기 시드에서 유도**하므로 두 브라우저가 같은 홈을 보고, 같은 시드면 언제나 같다. 혼자 하기도 사람이 원정일 수 있다.
- **코인토스 연출** — 시작 직후 3초, 동전이 돌다 멈추고 "🏠 홈 ○○ · 당신은 홈/원정입니다". 렌더 전용이라 온라인 락스텝을 멈추지 않는다.
- **경기장이 홈 팀 색으로** — 관중 절반이 홈 색 옷(밝기 세 단계), 바탕·스탠드 옆면도 홈 색의 어두운 버전. 홈이 바뀌면 캔버스를 다시 그린다.
- 전광판 팀 칸 색도 경기마다 갱신(`Hud.setColors`). 테스트 90 → **94개**(`tests/toss.test.ts` — 결정론·쏠림·구단 상관).

## 2026-09-11 — 그 외 명단에 구단 필터 · 문서 잔재 정정

- "전 구단"을 볼 때 **구단 셀렉트**가 생긴다 — 고르면 그 구단 선수만(구분선에도 구단명). "내 구단" 일 때는 안 보인다.
- 문서 잔재 정정 — `DESIGN 5.10`(저장 슬롯 10 + 코드 복사 버튼 → **슬롯 5 + JSON**, 코드는 P2P 전용), `DECISIONS N-4`(`col2` 사용 → 2026-09-10 단색), `DESIGN 7.2`(스쿼드 화면에 새 명단 배치), `player3d.ts` 주석.

## 2026-09-11 — 명단 배치 (KM26 방식) · 전술판 아래 후보 제거

- 오른쪽을 **명단 한 통**으로 다시 짰다 (KM26 `fmSquadTable` 배치) — `⚽ 선발 11명`(자리 순서) → `🪑 후보 7명` → `📋 그 외 명단`(선발·후보에 **없는** 선수만, 스크롤). 세 구역이 같은 열(자리 · 선수 · 종합 · 포지션·급여 · 능력치 눈금 · 버튼)을 쓴다.
- **전술판 아래 후보 칩을 없앴다** — 잘 보이지 않아서. 후보는 이제 명단 표에서 다룬다.
- 줄을 누르면 전술판과 **같은 자리가 골라진다**. 버튼으로 한 번에 — 선발 `⬇ 후보로`, 후보 `⬆ 선발로`, 그 외 `⬆ 후보로`(가장 약한 후보와 교체).
- KM26 처럼 **예비 골키퍼 없음** 경고를 후보 구분선에 띄운다.
- 상세 패널이 스크롤을 따라온다(sticky).

## 2026-09-11 — 스쿼드 화면 2차 (세로 전술판 · 칩 이름 · 별 폭 버그 · 능력치 눈금)

- **전술판을 세로로** — 자기 골문이 아래, 공격 방향 위. 칩은 이름 한 줄 + 별 + 자리·등번호 세 줄이라 이름이 잘리지 않는다. 셋이 나란한 줄의 가운데(CM·ST·CB·CAM·DM)는 살짝 아래로 어긋나 겹치지 않는다.
- **상세의 별이 전부 만점처럼 보이던 버그** — 별 span 이 그리드 칸에서 늘어나 금색 덮개(폭 %)가 별 다섯을 다 덮었다. `.stars { width: max-content }` 로 글자 폭만큼만.
- **능력치 눈금** — 목록의 막대 6개를 별과 같은 5단계(반 칸) **눈금 다섯 칸**으로, 머리줄은 **우리말**(속력 슈팅 패스 드리블 수비 피지컬 / 다이빙 핸들링 킥 반사 위치 스피드). 마우스를 올리면 "속력 3.5/5".
- **자동 채우기 띠를 전술판 바로 위로** (금색 테두리).

## 2026-09-11 — 로비 급여 게이지 고침

- 로비 스쿼드 띠의 급여 게이지가 5% 정도로만 차 보이던 것(사용자 제보) — 선수 목록 막대와 `.mini` 클래스가 겹쳐 목록의 24px 격자 규칙이 게이지에 먹었다. 목록 막대를 `.sixbars` 로 바꾸고, 게이지 옆에 "급여 67% 사용 · 남은 83" 라벨을 붙였다.

## 2026-09-10 — v0.8.1 스쿼드 화면 · 유니폼 (DECISIONS 10장)

- **자동 채우기 선발/후보 따로** + 기준 세그먼트(**포메이션 능숙도 우선 / 선수 능력치 우선**, 브라우저에 기억). 선발을 채워 후보가 비면 모자란 포지션부터 메운다.
- **OVR·세부 능력치를 숫자로 보이지 않는다** — 별 5개(반 개 단위). 칩·후보·목록·상세·구단 카드·대기실·강화 상자 전부. 급여·능숙도는 숫자 그대로.
- 선수 목록에 **항목명 머리줄**(PAC SHO PAS DRI DEF PHY / GK 는 DIV HAN KIC REF POS SPD) + 행마다 **미니 막대 6개**. 상세는 항목명 + 우리말 + 별.
- **JSON 저장 / 불러오기**(파일)로 스쿼드를 주고받는다. 코드 복사/붙여넣기는 화면에서 뺐다(P2P 대기실 내부용으로만 남는다).
- **저장 안 하고 돌아가기** — 화면을 열 때의 스쿼드로 되돌린다. "이 스쿼드로 (저장)"과 짝.
- **유니폼 단색** — 홈은 구단 색 하나로 상하의·양말, **원정은 골키퍼 빼고 전신 흰색**. 구단 색 띠(로비·대기실·구단 카드)도 단색.

## 2026-09-10 — v0.8.0 사용자 제보 11건 (DECISIONS 9장)

- **골키퍼 순간이동 제거** — 잡기는 손 거리(서서 0.8 · 날면서 1.0 m)에서만. 옆으로 지나는 공은 반응 뒤 그 경로로 **몸을 날린다**(속도 있는 다이브, 앞 12틱 비행). 못 닿으면 들어간다. **백패스 룰** — 아군이 발로 준 공은 손으로 못 잡는다.
- **방향 표시** — 방향키를 누르면 조작 선수 앞에 `›`. 공격 노랑 · 슛 모으면 붉고 길어짐 · 수비 흰색.
- **킥오프는 아군에게 짧은 패스로만** (D 를 눌러도). 차는 순간 휘슬 + 자막 "전반/후반 킥오프".
- **수비 키** — D 홀드 = 공을 향해 자동 추격(방향키로 옆 조정) · Space = 공 쪽 돌진 태클 · A = 방향키 없으면 공 쪽 슬라이딩 · **W = 골키퍼 돌진** · C 견제는 마주 보고 들이받는 드리블 ×1.5.
- **슛 보조** — 슛은 언제나 골문을 겨눈다. 방향키 위/아래 = 코너, 안 누르면 GK 가 비운 코너. 뒤로 밀고 차면 오차만 ×1.6.
- **추가시간** — 죽어 있던 시간을 모아 1~5분 발표(`45'+2`). 추가시간 뒤 공이 죽었을 때 끝내고, 공격 중이면 최대 20초 기다린다. 하프타임 자막 "전반 종료".
- **수비 강화** — 가로채기 반경(posn)·성공(mark)·**슛 블록**(posn/tck, `block` 사건)·마크 거리(mark)·압박 예측(posn).
- **선수 모델 마르게** — 몸통·팔다리 굵기 ×0.8.
- **스쿼드 화면** — 저장 슬롯 10 → **5칸 띠**(맨 위) · 급여를 큰 숫자 + 남은 급여 · 선수 목록마다 **바꾸면 ±급여 · 상한 초과** 표시(초과는 못 고름) · **강화 상자**(설명 · OVR 지금→다음 · 자동 배분 셋).
- **로비 스쿼드 띠** — 구단 색·급여 게이지·큰 버튼. **대기실** — 양쪽 구단 색·선발 OVR·급여·팀컬러·강화.
- 문서 — `docs/플레이-가이드.md` 신설 · `docs/공지글-모음.md` 1차 문안.
- 계측(합성 40판) — 평균 3.10골 · 유효슛 9.8 · 선방 7.0 · 패스 47%. 파울 17.5/판은 많다(단계 7 후속). 테스트 90개.
- `verify 200` — 지문 4/4 · 팀컬러 +11.8%p ✅ · 강화 수비 +2.7%p(−0.3 에서) · 공격 +2.0%p(7.8 에서 ↓) △ · 능숙도 87.3% ✅ · 결정론 100/100 ✅ · 홈/원정 42.5% ⚠ → `npm run asym 100`(새 도구, 하프별 진단)에서는 51.7% — 시드 노이즈로 판단.

## 2026-09-09 — v0.7.0 원본 명단 갱신 (KM26 오늘자) — 1,024 → 1,056명

개리그 매니저(KM26)가 오늘 로스터·능력치를 고쳤다. 그것을 그대로 받아 왔다.
원본 `github.com/KleagueM2026/KM26v2.0` 커밋 `0a08e86`(01:17) · `547f422`(05:25).

### 무엇이 바뀌었나

| | 예전 | 새 |
|---|---|---|
| 선수 | 1,024명 | **1,056명** (+32) |
| 구단 | 29 (1부 12 · 2부 17) | 그대로 — 부·이름 변동 없음, **구단 색 6곳** 갱신 |
| dataHash | `6d4b32c636580eae` | `b53cd9ed65ffe691` |
| `POOL_HASH` | — | **57542 로 바뀜** |
| 급여 상한 | 238 | **252** |
| OVR 보정 | a=1.6794 b=−49.02 | a=1.6236 b=−44.00 |
| OVR 최대 | 95 | 91 |

명단은 29개 구단 전부에서 조금씩 움직였다 (김천 +6 · FC서울 +5 · 천안 +4 · 전북 +4 …
안양 −2 · 김포 −1 …). 어느 구단도 18명 아래로 내려가지 않았다.

**저장된 스쿼드는 전부 버려진다.** `POOL_HASH` 가 바뀌었기 때문이고, 일부러 그렇게 만든 장치다 —
저장 스쿼드는 선수 **id** 만 갖고 있어서, 명단이 바뀌면 같은 id 가 딴 선수를 가리킨다
(v0.6.0 의 "골키퍼 자리에 공격수" 사고가 바로 그것이었다). 돌아다니던 스쿼드 **코드**도 같다.

### 급여 상한·OVR 보정은 손대지 않았다

둘 다 카드 풀에서 **런타임에** 다시 뽑는다 (`calibrateOvr` · `computeCap`). 상한이 238 → 252 로
움직인 것도 코드를 고쳐서가 아니라 데이터가 바뀌어서다. 29개 구단 전부 자기 선수 18명으로
상한 안에 든다 (가장 빠듯한 FC 서울 244/252, 가장 여유로운 파주 64/252).

### 카드 장수를 더 이상 못 박지 않는다

`1024` 를 코드·테스트·화면에 박아 두었더니 이번에 전부 걸렸다. 원본 명단은 앞으로도 움직인다.
- 테스트: 장수 대신 **깨지면 안 되는 것**을 본다 — 구단 29 · 구단마다 18명 이상 + GK 1명 이상 ·
  id 중복 없음 · id 가 11비트(스쿼드 코드 규격) 안.
- 화면: 로비·스쿼드의 "1,024장" 을 `POOL_SIZE` 에서 읽는다.

### 추출 파이프라인 (KMD26 도구)에서 걸린 것 둘

1. **패치 GK-01 이 0곳** — 원작자가 `assignPrefPos` 에 같은 고침을 위에서 넣었다
   (골키퍼는 GK, 표에 GK 가 남아 있으면 무시). KMD26 패치와 의미가 같아 뺐다.
2. **`UID_GEN is not defined`** — 새 `nextGenUid` 가 그 상수를 본다. 그런데 선언이
   `const UID_CLUB=1001, UID_COACH=2001, UID_REAL=10000, UID_GEN=900000;` 한 줄에 얹혀 있고
   `jsclosure.collect_decls` 는 다중 선언의 **첫 이름만** 색인한다. 뿌리에 `UID_CLUB` 을 넣어 해결.

되풀이할 수 있게 HANDOVER 에 파이프라인 전체를 적어 두었다.

### 계측 — 무엇이 실제로 데이터를 타는가

`npm run verify` · `oneone` · `balance` 는 **합성 스쿼드**(`synthSquad`)끼리 붙인다. **엔진을 재는
것이지 카드 데이터를 재는 게 아니다.** 명단을 갈고 다시 재도 소수점까지 같은 값이 나왔다
(팀컬러 +10.0%p · 홈/원정 51.5% · 능숙도 82.8% · 1대1 38.8% · 결정론 100/100).
카드 데이터를 실제로 타는 것은 `npm run clubs` · `npm run cards` · 테스트뿐이다.

| 실제 구단 계측 | 예전 | 새 |
|---|---|---|
| 90판 평균 골 | 3.10 | 3.56 |
| 1부 vs 2부 승률 | 60.2% | **70.5%** |

1부/2부 격차가 벌어진 것은 원본이 그렇기 때문이다 (FC서울 급여 244 vs 파주 64). 버그가 아니다.

덧붙여, `verify` 를 기본 표본(120)으로 돌리면 홈/원정 56.7% · 수비 강화 −7.1%p 가 나오는데
둘 다 노이즈다. 시드가 `5000+i` 고정이라 N 이 바뀌면 표본 자체가 바뀐다 — **항상 200 으로 잰다.**

## 2026-09-09 — v0.6.1 스쿼드 수정 (선수 · 자리 · 선발↔벤치)

구단을 다시 고르지 않고 **바꾸고 싶은 것만 바꾸는** 흐름을 붙였다 (사용자 요청).
바꾸는 세 가지가 전부 원래도 됐지만, 어디를 눌러야 하는지가 보이지 않았다.

- **로비 버튼을 둘로** — "스쿼드 수정"(전술판으로 바로) · "구단 바꾸기"(구단 고르기로).
  저장된 스쿼드가 있으면 수정으로 열리고, 없으면 어느 쪽을 눌러도 구단 고르기로 간다.
- **바꿀 수 있는 자리를 보여 준다** — 자리를 고르면 맞바꿀 수 있는 자리에 **초록 테두리**,
  안 되는 자리는 흐려진다. 골키퍼를 고르면 벤치 골키퍼 한 명만 초록이 된다.
- **선발↔벤치 한 번에** — 오른쪽 카드에 "벤치로 내리기 / 선발로 올리기" 버튼.
  짝은 자동으로 고른다. 선발을 고르면 **그 자리 능숙도×2 + OVR** 이 가장 높은 벤치 선수,
  벤치를 고르면 **능숙도가 높고 지금 앉은 사람이 약한** 선발 자리다. 짝이 없으면 버튼이 꺼진다.
- **전술판 아래 한 줄 안내** — 자리 옮기기 · 선발↔벤치 · 선수 교체 셋을 적어 뒀다.
- 구단 고르기에서 **"그만두고 스쿼드 수정으로"** — 실수로 들어왔을 때 스쿼드를 지키고 돌아간다.

## 2026-09-09 — v0.6.0 스쿼드 화면 개편 (구단 먼저 · 전술판) + 골키퍼 자리 버그

### 버그 — 골키퍼 자리에 공격수가 앉아 있었다 (사용자 제보)
저장된 스쿼드는 선수 **id** 만 갖고 있는데, 카드 데이터를 자리표시자에서 실제 명단으로 바꾸면서
같은 id 가 딴 선수를 가리키게 됐다. `loadSquad` 가 길이와 포메이션만 보고 그대로 받아들여
골키퍼 자리에 공격수가 앉았다.
- 저장 스쿼드에 **데이터 지문(`hash`)** 을 같이 넣고, 다르면 버린다. 저장 슬롯 10칸도 같다.
  (스쿼드 **코드**에는 원래 같은 지문이 박혀 있었다 — 저장만 빠져 있었다, DESIGN 5.9)
- 읽을 때 `checkSquad` 도 돌린다. 규칙을 어기면 버리고 다시 짜게 한다.
- **골키퍼 자리(선발 0번)에는 골키퍼만** 설 수 있게 막았다. 필드 자리에는 골키퍼를 못 넣는다(벤치는 자유).
  전술판에서 자리를 맞바꿀 때도 같은 규칙이라 조작으로 깨뜨릴 수 없다.

### 스쿼드 화면 개편 (사용자 요청 — 개리그 매니저·듀얼 참고)
1. **구단을 먼저 고른다** — 29개 구단을 구단 색·부·평균 OVR·최고 선수와 함께 보여 준다.
2. 고르면 **그 구단 선수로 선발 11 + 벤치 7 을 자동으로 채운다** (팀컬러 +4).
3. **전술판** — 초록 피치에 센터 서클·페널티 박스·골 에어리어·골대를 그리고 포메이션 좌표(`SLOT_XY`,
   KM26·KMD26 과 같은 값)대로 선수를 앉힌다. 자리를 눌러 고른 뒤 다른 자리를 누르면 **서로 바뀐다**.
   끌어다 놓아도 된다. 카드 왼쪽 띠 색이 **그 자리 능숙도**다.
4. 선수 목록은 기본이 **내 구단 선수**다. "전 구단 1,024장" 으로 넓힐 수 있다(팀컬러는 깨진다).
   자리를 고르면 그 자리 **능숙도가 높은 선수가 위로** 오고, 넣을 수 없는 선수는 흐려진다.
5. "자동 채우기"로 언제든 그 구단 최적 스쿼드로 되돌린다.

## 2026-09-09 — v0.5.0 소리 · 테스트 모드 · 로비 개편

### 소리 (DESIGN 7.3) — 파일 없이 전부 코드로
샘플·음원을 하나도 쓰지 않는다. 오실레이터 + 노이즈 + 필터뿐이라 **저작권 문제가 없다** (`src/audio/sfx.ts`).
- **관중석** — 필터 건 잡음이 계속 흐르고, 공이 골문에 가까울수록 두꺼워진다. 소리만 듣고도 위험한 줄 안다.
- **휘슬** — 2.6·3.3 kHz 두 음을 42 Hz 로 떨어 만든 트릴. 파울·킥오프는 한 번, 하프·종료는 세 번(마지막이 길다).
- 킥(파워 반영) · 스로인(손이라 부드럽다) · 태클 · 선방 · 골대(금속 배음) · 골(함성) · 경고/퇴장 · 화면 조작음.
- **로비 배경음** — 느린 화음 패드 + 아르페지오. 4마디 진행을 오실레이터로 만든다. 경기에 들어가면 끄고 관중석만 남긴다.
- 소리 위치는 공 x 로 좌우 팬. 음소거는 경기 화면 버튼·Esc 메뉴·설정 어디서나.
- **크기를 계측으로 맞췄다** — 파형 RMS 로 재서 골 0.051 · 휘슬 0.045 · 태클 0.038 · 골대 0.031 · 슛 0.025 · 스로인 0.017
  (관중석 바닥 0.004). 처음엔 골대가 0.111 로 튀고 스로인이 0.0035 로 안 들렸다.
- 골대를 맞으면 `post` 이벤트를 남긴다 (물리는 그대로 — 결정론에 영향 없음).

### 테스트 모드 (사용자 요청) — 배포에서는 안 보인다
주소에 **`?test=1`** 을 붙였을 때만 로비에 보라색 타일이 뜬다. 링크를 안 걸면 그것으로 가려진다.
- **AI 대 AI 관전** — 두 팀 다 봇. 홈·원정 난이도를 따로 줄 수 있다.
- **키 입력 표시** (`src/render/keyview.ts`) — 지금 눌린 물리 키와 sim 으로 나가는 `Input` 비트를 화면에 띄운다.
  키가 안 먹는지 · 조합(Q+D)이 잡히는지 · 공수에 따라 뜻이 바뀌는지를 눈으로 본다.

### 로비 개편 (사용자 요청)
혼자 하기 · 방 만들기 · 설정을 **큰 버튼 셋**으로 빼고, 누르면 **팝업**이 뜬다 (bedorage-duck 방식).
맨 위에 내 스쿼드 한 줄 + 스쿼드 짜기 버튼, 아래에 방 목록. Esc·바깥 클릭으로 팝업이 닫힌다.

## 2026-09-09 — v0.4.0 단계 1 · 실제 선수 데이터 (보호명)

원작자 허락을 받아 (PREP A-1) K리그 2026 **29개 구단 1,024명**을 넣었다. 자리표시자("가1-07")는 사라졌다.

- `tools/build_cards.py` — 원본 명단 → `src/data/cards.json`. 하는 일 셋:
  · **보호명** — KM26 `fictName` 그대로. 한국 선수는 성씨를 다른 실재 성씨로(김→구, 이→여 …),
    외국 선수는 첫 글자 초성을 민다. 결과가 **다른 실존 선수의 이름**이 되면 마지막 글자를 밀어 피한다(3건).
  · **구단명은 지역명만** — 기업 이름·구단 고유 별칭을 쓰지 않는다 (DESIGN 10.2). 표 한 곳(`CLUB_NAME`)만 고치면 된다.
  · **컬럼 형식**으로 굽는다 — 키를 1,024번 되풀이하지 않아 700 KB → 174 KB.
  끝에서 **실명이 한 건도 안 남았는지 직접 확인**하고 나서야 파일을 쓴다.
- `src/data/pool.ts` — 생성기가 아니라 `cards.json` 을 푼다. 구단 색·부(1/2)도 함께.
- **구단 스쿼드** — `clubSquad()` 로 한 구단 선수만 뽑아 팀컬러 +4 를 받는 순수 팀을 짤 수 있다.
  스쿼드 화면의 "구단 통째로 불러오기", 로비의 "상대 구단" 선택, 처음 여는 사람의 기본 스쿼드가 모두 이걸 쓴다.
- **유니폼이 구단 색** (`src/render3d/kits.ts`) — 상의 `col` · 소매/바지 `col2`.
  두 팀 색이 130 안쪽이면 원정이 흰 상의로 갈아입는다(조합의 36%). 전광판 글씨·레이더 점 색도 따라 바뀐다.
- 계측 — 실제 구단 90판 평균 **3.10골** (목표 2.5~3.5) · 0:0 3% · **1부가 2부를 60.2%** 로 이긴다 · 같은 부 홈 승률 48.8%.
- **급여 상한 버그를 계측이 잡았다** — 조건 2 를 포지션 몫(GK2/DF6/MF6/FW4)으로 재고 있어서
  4-3-3 처럼 공격수가 많은 포메이션이 상한을 넘었다(강원 231 > 230). 포지션을 가리지 않은 최고 18명으로 바꿔 **238**.
  이제 29개 구단 전부 자기 선수만으로 규칙을 통과한다 (테스트가 확인).
- 테스트 65 → **81개** (`tests/pool.test.ts` 16개 — 실명 없음·구단명에 기업명 없음·능력치 완전성·구단 스쿼드).

## 2026-09-09 — v0.3.0 단계 4~7 (규칙 · 카드/스쿼드 · 온라인 대전 · 밸런스 계측)

### 사용자가 지적한 축구 규칙 4건
- **킥오프 위치** — 전원 자기 진영, 차지 않는 팀은 센터 서클 밖. 매 틱 강제해서 조작으로도 못 어긴다.
- **골키퍼 보호** — 손에 든 공은 슬라이딩·압박으로 못 뺏는다. 도전 자체가 파울(골키퍼 차징) + 경고 확률.
- **골키퍼 롱킥** — `gkPunt` (55~72 m). 골킥에서 D 는 슛이 아니라 길게 차기.
- **스로인은 손으로** — 최대 14 m/s · 반드시 뜬다 · 직접 골 무효 · 던진 사람 두 번 터치 반칙.
- **심판 3명** — 주심(대각선 추적 · 카드) + 부심 2명(오프사이드 라인 · 깃발). 렌더 전용이라 결정론에 끼지 않는다.

### 단계 4 — 규칙
오프사이드(패스 순간 표시 → 관여하면 판정 · 스로인/골킥/코너 제외) · 파울 · 경고/퇴장(두 번째 경고) ·
직접 프리킥 · PK · 교체 3명/벤치 7명(다음 데드볼 적용) · 세트피스 위치를 매 틱 강제(벽 9.15 m · 스로인 2 m · 골킥은 박스 밖).
페이스 컨트롤을 **Ctrl → Shift** 로 (사용자 결정 — Ctrl+W 가 크롬 탭을 닫는다).

### 단계 5 — 카드 · 스쿼드
6스탯 · OVR(앵커 보정) · 급여표 · 급여 상한(도구가 데이터로: 265) · 팀컬러 · 강화 예산 24 ·
스쿼드 코드 `KLO26-…`(339비트 · 체크섬 10비트) · 스쿼드 화면 · 저장 슬롯 10.
카드 1,024장은 **자리표시자** — 실제 K리그 데이터는 허락 확인(PREP A-1) 뒤에.

### 단계 6 — 온라인 대전
Trystero 로비·방(정원 2) · 2인 락스텝(지연은 RTT 로) · 60틱 해시 대조 · 어긋나면 방장이 스냅샷 리싱크 ·
대기실(서로 스쿼드 코드를 주고받고 **받는 쪽이 다시 검사**) · 상대 이탈 시 그 시점 스코어로 종료.
로컬 두 탭 실측: 약 8,000틱 동안 양쪽 해시 전부 일치 · 리싱크 0 · 끊김 0. 일부러 어긋뜨리자 잡아내 되돌렸다.

### 단계 7 — 계측이 잡아낸 것
- **압박 슬라이더가 연결돼 있지 않았다.** 지문 검사 5시드 중 1시드만 달라졌다 → 붙는 거리·압박 인원·태클 시도를 함께 움직이게 고쳐 5/5.
- **멘탈리티가 지표를 안 움직였다** (38%) → 앵커 ±4→±6 m, 전진 패스 가중 → 슛 지표 64%.
- **오프사이드 판당 11.5회** → AI 가 라인을 지키고 오프사이드 위치로는 패스하지 않게 → 1.0회.
- **1대1 인데 골이 안 들어갔다** (사용자 제보) → 사람 슛이 정중앙(=골키퍼)을 겨누던 것 + 골키퍼가 반경 안이면 무조건 막던 것.
  이제 1대1 14 m 전환율 **39%**, 8 m **58%**.
- 봇전 120판: 평균 **2.72골** (목표 2.5~3.5) · 0:0 6% · 홈/원정 대칭 51.5% · 결정론 100/100 · 능숙도 초록 82.8%.
- 팀컬러 +4 는 승률 **+10.0%p** (목표 8~15%p). 강화 24 는 **공격수에 주면 +7.8%p, 수비수에 주면 0%p** — 어디에 쓰느냐가 갈린다.

테스트 26 → **64개**. 도구 `tools/verify.ts`(단계 7 검증) · `tools/oneone.ts`(1대1) · `tools/cards.ts` 추가.

## 2026-09-09 — v0.2.0 단계 3 · 조작·렌더·혼자 하기

- **브라우저에서 봇과 한 판이 된다.** 로비 → 혼자 하기(난이도 3단계 · 전후반 2/3/4분 · 포메이션 12종) → 경기 → 결과.
- `src/render3d/` — Three.js **방송 카메라**(사이드라인 바깥 38 m · 높이 30 m · 공 리드 6 m · 골문 앞 줌인 FOV 32→26 · 좁은 창은 세로 시야를 늘려 좌우를 자르지 않음) · **찰흙 선수**(프리미티브 조립 + `MeshToonMaterial` 3단 · 키 `h/180` · 굵기 `sqrt(w/75)` · 머리 4종 id 해시 · 등번호 캔버스 · 피부색 한 가지) · 코드 애니메이션(달리기·킥·슬라이딩·넘어짐·GK 다이브·GK 볼 들기) · 피치(줄무늬·라인 캔버스 텍스처) · 골대·네트 · 관중석 · 공 그림자 데칼 · 조작 표시(화살표·발밑 링·이름표).
- `src/game/` — `ticker.ts`(bedorage-duck Worker 타이머 그대로) · `localInput.ts`(**DESIGN 3.1 표준 키 배치 전부** — `e.code` 로 읽어 한글 IME 무관) · `session.ts`(60Hz 틱 + rAF 보간 · Esc 메뉴 · 결과표 · `beforeunload` 로 Ctrl+W 오작동 방지).
- `src/render/` — HUD DOM 오버레이(전광판·시계·단계 배너·조작 선수 카드·체력·파워 게이지·전술 프리셋·키 힌트) · 레이더.
- `src/ui/` — 로비 · 설정(그림자·해상도 75%·키 힌트, localStorage) · **폰 차단**(`maxTouchPoints`+`pointer: fine`, 폭 < 900px).
- 코어 — **사람 킥커의 리스타트 킥 방향** (`performRestartKick(st, aim)`: 방향키 + S/W/A/D, 없으면 AI 선택). 결정론 그대로.
- 테스트 26개 — 키 매핑 · 카메라 수학 · 리스타트 킥(사람 입력 결정론 포함) 추가.
- 확인한 것 — 스크립트로 흘려 넣은 키 입력으로 사람 팀이 **골을 넣었다**(40초 · 슛 4 · 골 1). Esc 메뉴 · 하프타임 진영 교체 · 종료 결과표 동작. 렌더 드로우콜 약 740(그림자 포함) · 삼각형 13만.
- 못 잰 것 — **60fps** 는 Claude 브라우저 패널이 뒤에 있으면 rAF 가 멈춰 못 쟀다. 사용자 PC 에서 우상단 fps 로 볼 것.
- 소리는 아직 없다 (단계 3 기준에 없어 뺐다).

## 2026-09-08 — v0.1.0 단계 2 · 시뮬 코어

- `src/core/` 9 파일 — 피치·공·선수 물리, 소유(붙임 모델)·태클·슬라이딩·패스(땅볼/스루/로빙/크로스)·슛(파워·칩)·걷어내기·GK(잡기/펀칭/배급), 팀 AI(앵커·지원 러닝·압박/커버/마크·볼 소유 효용·요격), 규칙(시계·하프·킥오프·골·스로인/골킥/코너), 사람 입력 해석(3.1 키 전부), 해시·스냅샷. 합성 스쿼드로 검증.
- 실측 — 같은 시드 해시 100% 일치 · 틱당 0.017 ms · 20판 평균 4.4골 · 슛 16 (유효 79%) · 패스 성공 40%. 골·유효슛은 단계 7 에서 조정.
- 겪은 것 — 빠른 공이 발을 뚫고 지나가던 것(선분 거리·요격 지점으로 해결), AI 가 잡자마자 되차던 것(20틱 보유).
- `npm run balance` · `npm run probe` · 테스트 10개.

## 2026-09-08 — v0.0.1 설계

- 설계서 `docs/DESIGN.md` v0.1 (13장) — 정체성·규칙·조작·결정론 시뮬·카드/스쿼드·네트워크·화면·구조·배포·비용/법적·개발 단계·검증·백로그.
- `docs/DECISIONS.md` — 사용자 결정 3건(직접 조작 · 전원 개방+급여 상한 · KLO26) · 기본안 17건 · 확인 필요 5건.
- `docs/PREP.md` — 원작자 허락 재확인 · 저장소 · Pages · 기기 · 비용 원칙.
- README · HANDOVER · 공지글 틀.
- 스캐폴드 — Vite + TypeScript + vitest + GitHub Pages 워크플로. `src/core/rng.ts`·`fixedmath.ts` (bedorage-duck 복사) + 테스트. 게임 코드 없음.
- 추가 결정 5건 반영 — **선수 이름 보호명**(빌드 단계 적용, 저장소에 실명 0건) · 경기 기본 3분 · **키보드만 검증**(패드 미검증) · 급여 상한 조건에 **K리그2 포함** · 공개 채널은 공개 시점에.
- 시점·렌더를 **Three.js 방송 카메라**로 (사용자 결정) — 찰흙 선수 · 키 `h/180` 반영 · 피부색 안 함(데이터 없음) · 닮게 그리지 않음. DESIGN 7.1 다시 씀, `package.json` 에 `three`.
- **PC 전용** (사용자 결정) — 터치 조작 없음, 폰·태블릿은 안내 화면만.
- **키 배치를 축구 게임 표준 키 그대로** (사용자 결정) — 방향키 · S/W/A/D/E · Space · C · Q · Ctrl · 조합키 · `[ ]`. 입력 비트도 키 이름으로. **게임패드는 계획에서 제외** — 백로그에도 없음, 첫 화면·README 에 명시.
- **제3자를 거명하는 고지 문구 전부 삭제** (사용자 결정) — NOTICE.md 삭제, README 고지 절 삭제, 첫 화면 문구 축소, 타 게임·회사 이름을 문서 전체에서 제거.
- 저장소 `goormigrm/KLO26` 첫 커밋·푸시. Pages(Actions) 켜고 첫 배포 — <https://goormigrm.github.io/KLO26/>.
